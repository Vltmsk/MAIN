# Глобальный план развития проекта

## Критически важные задачи

### 0. Нормализация отображения названий монет
**Проблема:** В системе названия монет отображаются в разных форматах (ETHUSDT, XRP_USDC, LTC-TRY), что создаёт неудобства для пользователей. Пользователи ожидают видеть нормализованные названия монет (ETH, XRP, LTC) везде на сайте.

**Текущая ситуация:**
- Внутри системы (сбор свечей, детектирование, БД) символы хранятся в формате биржи: `BTCUSDT`, `ETHUSDT`, `XRP_USDC`, `LTC-TRY` и т.д.
- На веб-интерфейсе символы отображаются в том же формате, что создаёт путаницу
- Разные биржи используют разные форматы разделителей (без разделителя, подчёркивание, дефис)

**Требования к реализации:**

1. **Принцип работы:**
   - Внутри системы (БД, детектирование, сбор свечей) символы остаются в исходном формате биржи
   - На веб-интерфейсе и в API ответах символы отображаются в нормализованном виде (только базовая монета: ETH, XRP, LTC)
   - Нормализация должна быть обратимой: из нормализованного символа можно восстановить исходный формат для запросов к БД

2. **Места, где нужно применить нормализацию:**
   - **API endpoints** (`api_server.py`):
     - `/api/spikes` - в ответах со списком стрел
     - `/api/users/{user}/spikes` - в ответах со стрелами пользователя
     - `/api/users/{user}/spikes/by-symbol/{symbol}` - нормализация в URL и ответах
     - `/api/users/{user}/spikes/stats` - **КРИТИЧНО:** в статистике (top_symbols, top_by_delta, top_by_volume) - особенно важно для вкладки статистики
     - `/api/exchanges/stats` - в статистике по биржам
   - **Веб-интерфейс** (`WEB/app/(dashboard)/components/DashboardShell.tsx`):
     - **ВКЛАДКА СТАТИСТИКИ (КРИТИЧНО):**
       - Блок "Топ-10 символов" (top_symbols) - отображение списка монет с нормализованными названиями
       - Блок "Детали по монете" - заголовок с нормализованным названием монеты
       - Таблица деталей по монете - все символы должны быть нормализованы
       - "Топ 10 стрел по дельте" - символы в карточках должны быть нормализованы
       - "Топ 10 стрел по объёму" - символы в карточках должны быть нормализованы
     - Блок "Топ монет" (top_symbols) - отображение списка монет
     - Блок "Детали по монете" - заголовок и список стрел
     - Таблица стрел - колонка с символом
     - Чёрный список монет - отображение и ввод символов
     - Шаблоны сообщений - плейсхолдер `{symbol}` или `[[Торговая пара]]`
   - **Telegram уведомления** (`core/telegram_notifier.py`):
     - В шаблонах сообщений символ должен отображаться в нормализованном виде
   - **Условные форматы сообщений (КРИТИЧНО):**
     - В условиях условных форматов можно использовать нормализованный формат криптовалют для **ВСЕХ монет на ВСЕХ биржах** (ETH, BTC, XRP, ADA, SOL, DOGE и любые другие)
     - Система должна понимать нормализованный формат и сопоставлять его со всеми вариантами символа на всех биржах:
       - Binance: `ETH` → `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д.
       - Gate: `ETH` → `ETH_USDT`, `ETH_BTC`, `ETH_USDC` и т.д.
       - Bitget: `ETH` → `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д.
       - Bybit: `ETH` → `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д.
       - Hyperliquid: `ETH` → `ETH` (linear) или `ETH/USDC` (spot)
     - При проверке условия по символу нужно нормализовать символ свечи и сравнить с условием
     - Поддержка как нормализованного формата (ETH), так и исходного (ETHUSDT, ETH_USDT) для обратной совместимости
     - Работает для всех монет, а не только для примеров

3. **Функция нормализации:**
   - Создать утилиту `normalize_symbol()` в отдельном модуле (например, `core/symbol_utils.py`)
   - Функция должна извлекать базовую монету из символа для **ВСЕХ монет на ВСЕХ биржах**
   - **Форматы символов на разных биржах:**
     - **Binance**: без разделителя (`BTCUSDT`, `ETHUSDT`, `ADAUSDT` и т.д.)
     - **Gate**: с подчёркиванием (`BTC_USDT`, `ETH_USDT`, `ADA_USDT` и т.д.)
     - **Bitget**: без разделителя (`BTCUSDT`, `ETHUSDT`, `ADAUSDT` и т.д.)
     - **Bybit**: без разделителя (`BTCUSDT`, `ETHUSDT`, `ADAUSDT` и т.д.)
     - **Hyperliquid**: разные форматы:
       - Linear: только базовая монета (`BTC`, `ETH`, `ADA` и т.д.) - без котируемой валюты
       - Spot: со слэшем (`PURR/USDC`, `BTC/USDC` и т.д.)
   - **Поддержка различных форматов разделителей:**
     - Без разделителя: `BTCUSDT`, `ETHUSDT`, `ADAUSDT`
     - Подчёркивание: `BTC_USDT`, `ETH_USDC`, `XRP_TRY`
     - Дефис: `LTC-TRY`, `BTC-USDT`
     - Слэш: `BTC/USDT`, `ETH/USDC`, `PURR/USDC`
   - **Поддержка различных котируемых валют:**
     - Стандартные: `USDT`, `USDC`, `TRY`, `BTC`, `ETH`, `BNB`, `EUR`, `GBP`, `AUD`, `BRL`, `TUSD`, `FDUSD`, `BIDR`, `TRX`, `DOGE`, `AEUR` и другие
     - Список должен быть расширяемым и обновляться при добавлении новых бирж
   - **Обработка edge cases:**
     - Символы без котируемой валюты (Hyperliquid linear: `BTC`, `ETH`)
     - Символы с нестандартными форматами
     - Символы, которые уже нормализованы (если передали `BTC`, вернуть `BTC`)
     - Символы, которые не соответствуют ожидаемому формату (вернуть исходный символ или логировать предупреждение)

4. **Обратная нормализация (денормализация):**
   - При фильтрации по символу в API (например, `/api/spikes?symbol=ETH`) нужно найти все варианты символа с разными котируемыми валютами на всех биржах
   - Или использовать исходный формат символа, если он передан в запросе
   - Для запросов по конкретному символу (`/api/users/{user}/spikes/by-symbol/{symbol}`) нужно поддерживать оба формата
   - **Важно:** При денормализации нужно учитывать формат каждой биржи:
     - Для Binance: `ETH` → `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д. (без разделителя)
     - Для Gate: `ETH` → `ETH_USDT`, `ETH_BTC`, `ETH_USDC` и т.д. (с подчёркиванием)
     - Для Bitget: `ETH` → `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д. (без разделителя)
     - Для Bybit: `ETH` → `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д. (без разделителя)
     - Для Hyperliquid: `ETH` → `ETH` (linear, без котируемой валюты) или `ETH/USDC` (spot, со слэшем)

5. **Структура реализации (основной подход - отдельная БД):**
   
   **5.1. Создание отдельной базы данных для нормализации:**
   - Создать отдельный файл БД: `BD/symbol_normalization.db`
   - Структура таблицы:
     ```sql
     CREATE TABLE symbol_normalization (
         id INTEGER PRIMARY KEY AUTOINCREMENT,
         exchange TEXT NOT NULL,
         market TEXT NOT NULL,
         original_symbol TEXT NOT NULL,
         normalized_symbol TEXT NOT NULL,
         created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
         updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
         UNIQUE(exchange, market, original_symbol)
     );
     CREATE INDEX idx_symbol_lookup ON symbol_normalization(exchange, market, original_symbol);
     CREATE INDEX idx_normalized_lookup ON symbol_normalization(normalized_symbol);
     ```
   - Преимущества: быстрый read-only доступ, не блокирует основную БД, масштабируемо
   
   **5.2. Создать модуль `core/symbol_utils.py` с функциями:**
   - `normalize_symbol(symbol: str, exchange: str, market: str) -> str` - нормализация символа:
     - Сначала проверяет отдельную БД (быстро, с индексом)
     - Если не найдено - использует алгоритмическую нормализацию
     - Сохраняет в БД для будущего использования (асинхронно)
   - `denormalize_symbol(normalized: str, exchange: str, market: str) -> List[str]` - получение всех вариантов символа для биржи/рынка
   - `is_normalized(symbol: str) -> bool` - проверка, является ли символ нормализованным
   - `symbols_match(symbol1: str, symbol2: str) -> bool` - проверка, соответствуют ли два символа одной монете (с учётом нормализации)
   - `populate_normalization_db()` - заполнение БД при первом запуске
   - `sync_normalization_db()` - синхронизация БД с актуальными символами бирж
   
   **5.3. Интеграция в систему:**
   - В `api_server.py` добавить нормализацию символов в ответах API (используя `normalize_symbol()`)
   - В `WEB/app/(dashboard)/components/DashboardShell.tsx` использовать нормализованные символы для отображения
   - В `core/telegram_notifier.py`:
     - Нормализовать символы перед подстановкой в шаблоны
     - **Добавить поддержку условия "symbol" в условных форматах сообщений:**
       - Добавить новый тип условия `type: "symbol"` в `_check_condition()`
       - При проверке условия нормализовать символ свечи и сравнить с нормализованным значением из условия
       - Поддержать как нормализованный формат (ETH), так и исходный (ETHUSDT) в условии
       - Если в условии указан нормализованный формат (ETH), условие должно срабатывать для всех пар с этой монетой (ETHUSDT, ETHBTC, ETH_USDC и т.д.)
   
   **5.4. Инициализация и обновление БД:**
   - При первом запуске: заполнить БД всеми символами со всех бирж
   - При старте системы: проверка новых символов, добавление отсутствующих
   - Периодическое обновление: фоновая задача (раз в день/неделю)
   - On-demand: при получении нового символа из WebSocket - добавить в БД асинхронно
   
   **5.5. Деплой на сервер:**
   - **Важно:** Файл `BD/symbol_normalization.db` НЕ должен быть в git (уже в `.gitignore` как `*.db`)
   - БД создастся автоматически на сервере при первом запуске после деплоя
   - Код должен автоматически:
     - Проверить существование БД при старте
     - Создать БД и таблицы, если их нет
     - Заполнить БД символами при первом запуске
   - На сервере БД создастся в папке `BD/` при первом вызове `normalize_symbol()` или при явной инициализации
   - Рекомендуется добавить явную инициализацию БД при старте `main.py` для гарантии создания

6. **Важные моменты:**
   - **НЕ менять формат символов в БД** - все символы остаются в исходном формате
   - **НЕ менять логику детектирования** - детектирование работает с исходными символами
   - **НЕ менять формат символов в WebSocket** - сбор свечей использует исходные символы
   - Нормализация применяется только на уровне представления (API ответы, веб-интерфейс, Telegram сообщения)
   - При фильтрации по символу нужно учитывать, что пользователь может ввести как нормализованный, так и исходный формат

7. **Тестирование:**
   - **КРИТИЧНО - Вкладка статистики:**
     - Проверить отображение нормализованных символов в блоке "Топ-10 символов"
     - Проверить заголовок "Детали по монете" с нормализованным названием
     - Проверить таблицу деталей по монете (все символы должны быть нормализованы)
     - Проверить карточки в "Топ 10 стрел по дельте" (символы нормализованы)
     - Проверить карточки в "Топ 10 стрел по объёму" (символы нормализованы)
     - Проверить переключение между личной и рыночной статистикой
     - Проверить клик по символу из топа и загрузку деталей
   - Проверить отображение символов во всех местах веб-интерфейса
   - Проверить работу фильтрации по символу (нормализованному и исходному формату)
   - Проверить работу статистики (top_symbols должен показывать нормализованные символы)
   - Проверить работу чёрного списка (можно вводить как нормализованный, так и исходный формат)
   - Проверить Telegram уведомления (символы должны быть нормализованы)
   - **КРИТИЧНО - Условные форматы сообщений:**
     - Проверить работу условия по символу с нормализованным форматом для разных монет (ETH, BTC, ADA, SOL и т.д.)
     - Проверить, что условие срабатывает для всех пар с указанной монетой на всех биржах:
       - Binance: `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д.
       - Gate: `ETH_USDT`, `ETH_BTC`, `ETH_USDC` и т.д.
       - Bitget: `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д.
       - Bybit: `ETHUSDT`, `ETHBTC`, `ETHUSDC` и т.д.
       - Hyperliquid: `ETH` (linear) или `ETH/USDC` (spot)
     - Проверить обратную совместимость (условие с исходным форматом `ETHUSDT`, `ETH_USDT` должно работать)
     - Проверить комбинацию условий (символ + дельта + объём)
     - Проверить работу с дополнительными парами (TRY, USDC, BTC, ETH как котируемые валюты) в условиях
     - Проверить работу для монет с разными форматами на разных биржах
   - Убедиться, что детектирование не сломалось (символы в БД остались в исходном формате)
   - Проверить работу с дополнительными парами (TRY, USDC и т.д.)
   - **КРИТИЧНО - Проверка на всех биржах:**
     - Протестировать нормализацию символов для всех бирж (Binance, Gate, Bitget, Bybit, Hyperliquid)
     - Проверить работу с разными форматами разделителей на каждой бирже
     - Проверить работу с разными котируемыми валютами на каждой бирже
     - Убедиться, что нормализация работает для всех монет, а не только для примеров (ETH, BTC, XRP)

**Приоритет:** Критически высокий (первая задача)

**Порядок реализации:**
1. **Создать отдельную БД для нормализации:**
   - Создать файл `BD/symbol_normalization.db` (будет создан автоматически)
   - Создать таблицу `symbol_normalization` с индексами (автоматически при первом запуске)
   - Добавить методы работы с БД в `BD/database.py` или создать отдельный модуль `BD/symbol_normalization_db.py`
   - **Важно для деплоя:**
     - Убедиться, что файл `*.db` в `.gitignore` (уже есть)
     - БД создастся автоматически на сервере при первом запуске
     - Добавить явную инициализацию БД при старте `main.py`
2. **Создать модуль `core/symbol_utils.py`:**
   - Реализовать алгоритмическую нормализацию (парсинг символов) для всех бирж
   - Реализовать функцию `normalize_symbol()` с проверкой БД и fallback на алгоритм
   - Реализовать функции заполнения и синхронизации БД
   - Протестировать на реальных символах со всех бирж
3. **Инициализация БД:**
   - Реализовать функцию `populate_normalization_db()` для первоначального заполнения
   - Заполнить БД всеми символами со всех бирж при первом запуске
   - Добавить проверку новых символов при старте системы
4. **Интеграция нормализации:**
   - Добавить нормализацию в API endpoints (`api_server.py`)
   - Обновить веб-интерфейс для использования нормализованных символов
5. **Обновить Telegram уведомления:**
   - Нормализовать символы перед подстановкой в шаблоны
   - **Добавить поддержку условия "symbol" в условных форматах сообщений** (критично)
6. **Обновить веб-интерфейс:**
   - Поддержка условия "symbol" в условных форматах (добавить поле выбора монеты)
7. **Добавить периодическое обновление БД:**
   - Фоновая задача для синхронизации (раз в день/неделю)
   - On-demand обновление при получении новых символов из WebSocket
8. **Тестирование всех компонентов:**
   - Протестировать на всех биржах (Binance, Gate, Bitget, Bybit, Hyperliquid)
   - Протестировать с разными монетами (не только ETH, BTC, XRP, но и ADA, SOL, DOGE и т.д.)
   - Протестировать условные форматы с нормализованными символами на всех биржах
   - Проверить производительность БД (скорость чтения)
   - Проверить синхронизацию БД при добавлении новых монет

---

## Критические исправления

### 1. Исправление работы глобальных настроек на дополнительных парах
**Проблема:** При изменении глобальных настроек (например, на Binance Spot), дополнительные пары используют глобальные настройки вместо своих собственных настроек. В таблице "Активные фильтры" отображаются правильные индивидуальные настройки для дополнительных пар (например, Binance Spot TRY: 2%, 400к), но система детектирует стрелы по глобальным настройкам (1.5%, 200к).

**Техническая суть проблемы:**
- В `core/spike_detector.py` метод `_check_thresholds()` проверяет только глобальные настройки из `exchangeSettings[exchange][market]`
- Не проверяются индивидуальные настройки из `pairSettings` для конкретных пар
- При сохранении настроек `pairSettings` корректно сохраняются в БД, но не используются при детектировании

**Требования к исправлению:**

1. **Логика приоритета настроек при детектировании:**
   - Если для конкретной пары (например, TRY) есть индивидуальные настройки в `pairSettings` (ключ формата: `binance_spot_TRY`) — использовать их для детектирования этой пары
   - Если для конкретной пары нет индивидуальных настроек в `pairSettings` — значит пользователь отключил эту пару, детектирование для неё не выполняется
   - Если для биржи/рынка есть хотя бы одна дополнительная пара с настройками в `pairSettings` — глобальные настройки "Все торговые пары" (`exchangeSettings[exchange][market]`) НЕ применяются вообще для этого рынка

2. **Изменения в коде:**
   - Модифицировать метод `_check_thresholds()` в `core/spike_detector.py`:
     - Добавить проверку наличия индивидуальных настроек для конкретной пары из `pairSettings`
     - Если найдены индивидуальные настройки — использовать их
     - Если индивидуальных настроек нет — проверить, есть ли вообще дополнительные пары для этого рынка:
       - Если есть хотя бы одна дополнительная пара — не применять детектирование (пара отключена)
       - Если дополнительных пар нет — использовать глобальные настройки `exchangeSettings[exchange][market]`
   - Убедиться, что `pairSettings` корректно передаются в метод `_check_thresholds()` через `user_options`

3. **Структура данных:**
   - `pairSettings` хранятся в `options_json` пользователя в формате:
     ```json
     {
       "pairSettings": {
         "binance_spot_TRY": { "enabled": true, "delta": "2", "volume": "400000", "shadow": "70" },
         "binance_futures_USDC": { "enabled": true, "delta": "1", "volume": "100000", "shadow": "70" }
       }
     }
     ```
   - Ключ формируется как: `{exchange}_{market}_{pair}` (например: `binance_spot_TRY`)

4. **Тестирование:**
   - Проверить детектирование для пар с индивидуальными настройками
   - Проверить, что глобальные настройки не применяются, если есть дополнительные пары
   - Проверить, что пары без индивидуальных настроек не детектируются, если для рынка есть дополнительные пары

**Приоритет:** Высокий

---

## Улучшения функциональности

### 2. Улучшение блока "Условные форматы сообщений"
**Проблема:** Текущее отображение и работа с блоком "Условные форматы сообщений" требует улучшения.

**Текущие проблемы:**
- Шаблоны не имеют названий/описаний, отображаются только как "Условие #1, #2..."
- Нет возможности временно отключить шаблон без удаления
- Нет предварительного просмотра шаблона сообщения перед сохранением
- Нет фильтрации по биржам, рынкам или конкретным торговым парам
- Только AND логика (все условия должны выполняться), нет поддержки OR
- Нет валидации и предупреждений о конфликтующих условиях
- Сложная навигация при большом количестве шаблонов

**Задачи по улучшению:**

#### 2.1. UI/UX улучшения

1. **Названия и описания шаблонов:**
   - Добавить поле "Название шаблона" для каждого условного формата
   - Система должна автоматически генерировать краткое описание на основе выставленных параметров условий
   - Отображать название и автоматически сгенерированное описание вместо "Условие #1, #2..." в списке

2. **Управление шаблонами:**
   - Добавить переключатель включения/отключения для каждого шаблона (без удаления)
   - Добавить возможность изменения порядка шаблонов (drag & drop)
   - Добавить поиск/фильтрацию шаблонов по названию

3. **Предварительный просмотр:**
   - Добавить блок "Предварительный просмотр" с примером форматированного сообщения
   - Показывать, как будет выглядеть сообщение при выполнении всех условий
   - Обновлять предпросмотр в реальном времени при изменении шаблона

4. **Визуализация условий:**
   - Добавить визуальное отображение логики условий (AND/OR)
   - Показывать статус выполнения каждого условия (для тестирования)
   - Добавить индикаторы валидности условий (зеленый/красный)

5. **Улучшение отображения:**
   - Сделать блоки шаблонов сворачиваемыми (collapsible)
   - Добавить подсветку активных шаблонов
   - Улучшить визуальную иерархию условий

#### 2.2. Функциональные улучшения

1. **Расширение типов условий:**
   - Добавить условие "Тень свечи" (wick_pct) с диапазоном значений
   - Добавить условие "Биржа" (выбор конкретных бирж: Binance, Gate, Bitget и т.д.)
   - Добавить условие "Тип рынка" (Spot, Futures/Linear)
   - Добавить условие "Торговая пара" (фильтр по конкретным парам, например: BTCUSDT, ETHUSDT)
   - Добавить условие "Направление стрелы" (вверх/вниз)

2. **Логика условий:**
   - Добавить поддержку OR логики между условиями (хотя бы одно должно выполняться)
   - Добавить группы условий с разной логикой (AND внутри группы, OR между группами)
   - **Важно:** При срабатывании должны детектироваться ВСЕ подходящие шаблоны одновременно (без приоритетов)

3. **Валидация и проверки:**
   - Добавить валидацию условий перед сохранением
   - Показывать предупреждения о конфликтующих условиях (например, дельта < 0.5% и > 1%)
   - Добавить проверку на дубликаты шаблонов
   - Валидация шаблона сообщения (проверка наличия плейсхолдеров)

4. **Тестирование и отладка:**
   - Добавить функцию "Тестовый запуск" для проверки шаблона на исторических данных
   - Показать статистику срабатывания шаблонов (сколько раз использовался каждый шаблон)
   - Добавить логирование срабатывания шаблонов в отдельную таблицу БД

5. **Интеграция с дополнительными парами:**
   - Обеспечить корректную работу условных форматов на дополнительных парах (TRY, USDC и т.д.)
   - Добавить возможность применить шаблон только к определенным дополнительным парам

#### 2.3. Технические улучшения

1. **Оптимизация кода:**
   - Рефакторинг логики проверки условий в `core/telegram_notifier.py`
   - Оптимизация проверки множественных условий
   - Кэширование результатов проверки условий

2. **Производительность:**
   - Оптимизация проверки серий стрел для большого количества шаблонов
   - Добавить индексацию в БД для статистики срабатывания шаблонов

3. **Обратная совместимость:**
   - Обеспечить миграцию существующих шаблонов в новый формат
   - Поддержка старых форматов условий (volume, delta без диапазонов)

**Приоритет:** Средний

**Статус:** ✅ Выполнено (частично)

**Что реализовано:**
- ✅ Названия и автоматическая генерация описаний шаблонов (2.1.1)
- ✅ Переключатель включения/отключения для каждого шаблона (2.1.2)
- ✅ Предварительный просмотр шаблона сообщения с примерными данными (2.1.3)
- ✅ Расширение типов условий: добавлены wick_pct (тень свечи), exchange (биржа), market (тип рынка), direction (направление стрелы) (2.2.1)
- ✅ Обновлен backend для поддержки новых типов условий и флага enabled
- ✅ Обновлена логика сохранения и загрузки для новых полей

**Что осталось:**
- ⏳ Валидация условий и предупреждения о конфликтах (2.2.3) - частично (базовая валидация есть)
- ⏳ OR логика между условиями (2.2.2)
- ⏳ Группы условий с разной логикой (2.2.2)
- ⏳ Статистика срабатывания шаблонов (2.2.4)
- ⏳ Drag & drop для изменения порядка шаблонов (2.1.2)
- ⏳ Поиск/фильтрация шаблонов (2.1.2)
- ⏳ Тестовый запуск на исторических данных (2.2.4)

**Порядок реализации:**
1. ✅ Названия и автоматическая генерация описаний шаблонов (2.1.1)
2. ✅ Переключатель включения/отключения (2.1.2)
3. ✅ Предварительный просмотр (2.1.3)
4. ✅ Расширение типов условий (2.2.1)
5. ⏳ Валидация условий (2.2.3) - частично
6. ⏳ Остальные улучшения по приоритету

---

## Улучшения интерфейса

### 3. Подсказка для получения Chat ID
**Проблема:** Пользователям неочевидно, как получить Chat ID для настройки Telegram уведомлений. Текущая подсказка "Получите Chat ID через @userinfobot в Telegram" слишком краткая и не содержит пошаговой инструкции. Также не указано, какой именно ID нужно использовать (Your ID или Current chat ID).

**Текущая ситуация:**
- В поле "Chat ID" есть только краткая текстовая подсказка с упоминанием @userinfobot
- Нет визуального оформления и пошаговой инструкции
- Пользователи могут не знать, что делать после перехода к боту
- Не указано, что для групп нужно использовать "Current chat ID", а не "Your ID"
- Нет информации о том, что бот @getmyid_bot автоматически отправляет сообщение при добавлении в группу
- **Проблема с каналами:** Пользователи могут столкнуться с ситуацией, когда бот не отправляет сообщение после добавления в канал, потому что у него нет прав на отправку сообщений. Нет инструкции о том, как настроить права бота в канале

**Требования к реализации:**

1. **Места размещения подсказки:**
   - **Основное место:** Блок "Настройки Telegram" в `DashboardShell.tsx` (рядом с полем "Chat ID")
   - **Дополнительно:** В условных форматах сообщений, где можно указать отдельный Chat ID для шаблона
   - **Валидация:** При ошибке валидации Chat ID показывать ссылку на инструкцию
   - **Bot Token:** Подсказка для поля "Bot Token" должна включать важное предупреждение о том, что этого бота нужно использовать только для сигналов с этого сайта

2. **Варианты UI/UX реализации (рекомендуется комбинированный подход):**

   **2.1. Раскрывающийся блок с инструкцией (основной вариант):**
   - Добавить кнопку/ссылку "Как получить Chat ID?" рядом с полем ввода
   - При клике раскрывается блок с пошаговой инструкцией
   - Блок можно свернуть/развернуть
   - Визуально оформлен как информационный блок (иконка, фон, отступы)
   
   **2.2. Модальное окно с подробной инструкцией (альтернативный вариант):**
   - Кнопка "Инструкция" открывает модальное окно
   - В модальном окне: пошаговая инструкция с скриншотами/описанием
   - Кнопка "Понятно" для закрытия
   - Можно добавить чекбокс "Больше не показывать" (сохраняется в localStorage)
   
   **2.3. Tooltip с краткой информацией:**
   - При наведении на иконку вопроса показывать краткую подсказку
   - Ссылка "Подробнее" для открытия полной инструкции

3. **Содержание инструкции:**

   **3.1. Основной способ (для групп и каналов):**
   
   📋 **Пошаговая инструкция:**
   
   1️⃣ **Добавьте бота в группу/канал**
      - Откройте Telegram и найдите бота **@getmyid_bot**
      - Добавьте бота в группу или канал, куда вы хотите получать уведомления о детектах
   
   2️⃣ **Настройте права бота (для каналов):**
      - ⚠️ **Важно для каналов:** Если вы добавляете бота в канал, ему нужны права администратора для отправки сообщений
      - Перейдите в настройки канала → "Администраторы" → найдите бота @getmyid_bot
      - Дайте боту права администратора (или хотя бы право на отправку сообщений)
      - Без этих прав бот не сможет отправить сообщение с Chat ID
   
   3️⃣ **Получите Chat ID автоматически**
      - Бот автоматически отправит сообщение сразу после добавления (если у него есть права)
      - Если сообщение не пришло — проверьте права бота в настройках канала
      - В сообщении вы увидите:
        ```
        Your ID: 2065581586
        Current chat ID: -1003763476778
        ```
   
   4️⃣ **Скопируйте Chat ID**
      - Найдите строку **"Current chat ID"** в сообщении бота
      - Скопируйте число (например: `-1003763476778`)
      - Это и есть ваш Chat ID для группы/канала
   
   5️⃣ **Вставьте в поле**
      - Вставьте скопированное число в поле "Chat ID" выше
      - Сохраните настройки
   
   **💡 Важно:**
   - Используйте **"Current chat ID"**, а не "Your ID"
   - Для групп и каналов Chat ID всегда отрицательный (начинается с `-100`)
   - Для личных чатов можно использовать "Your ID" (положительное число)
   - ⚠️ **Для каналов:** Бот должен иметь права администратора или право на отправку сообщений, иначе он не сможет отправить Chat ID
   
   **3.2. Для личного чата:**
   
   📋 **Альтернативный способ:**
   
   1️⃣ **Напишите боту в личку**
      - Откройте Telegram и найдите бота **@getmyid_bot**
      - Напишите боту любое сообщение или отправьте `/start`
   
   2️⃣ **Получите ваш ID**
      - Бот автоматически отправит вам сообщение с вашим ID
      - Используйте значение из строки **"Your ID"** (положительное число)
   
   3️⃣ **Вставьте в поле**
      - Скопируйте число и вставьте в поле "Chat ID" выше
   
   **3.3. Важные примечания:**
   - ✅ **Chat ID для групп/каналов** — отрицательное число (например: `-1003763476778`)
   - ✅ **Chat ID для личных чатов** — положительное число (например: `2065581586`)
   - ✅ Формат: от 8 до 20 цифр
   - ⚠️ Не путайте "Your ID" и "Current chat ID" — для групп используйте "Current chat ID"
   - ⚠️ Не путайте Chat ID с Username (username начинается с @)
   - ⚠️ **Проблема с каналами:** Если бот не отправил сообщение после добавления в канал, проверьте его права:
     - Перейдите в настройки канала → "Администраторы"
     - Найдите бота @getmyid_bot и дайте ему права администратора
     - Или хотя бы включите право "Отправлять сообщения"
     - После этого бот сможет отправить Chat ID

4. **Визуальное оформление:**

   **4.1. Раскрывающийся блок:**
   - Иконка информации (ℹ️ или Info icon)
   - Заголовок: "Как получить Chat ID?"
   - Фон: `bg-zinc-800/50` или `bg-emerald-900/20`
   - Граница: `border border-zinc-700` или `border-emerald-700`
   - Скругление: `rounded-lg`
   - Отступы: `p-4`
   - Анимация раскрытия/сворачивания (transition)
   
   **4.2. Стилизация шагов инструкции:**
   - Нумерованный список или карточки с шагами
   - Иконки для каждого шага (1️⃣, 2️⃣, 3️⃣ и т.д.)
   - Выделение важной информации (жирный текст, цвет)
   - Кнопки-ссылки на ботов (открываются в Telegram)
   
   **4.3. Примеры Chat ID:**
   - Показать примеры правильного формата
   - Визуально выделить положительные и отрицательные числа

5. **Техническая реализация:**

   **5.1. Компонент инструкции:**
   - Создать отдельный компонент `ChatIdHelp.tsx` в `WEB/components/`
   - Компонент должен быть переиспользуемым
   - Поддержка раскрывающегося блока и модального окна
   
   **5.2. Интеграция в DashboardShell.tsx:**
   - Импортировать компонент `ChatIdHelp`
   - Разместить рядом с полем "Chat ID"
   - Передать пропсы для кастомизации (размер, стиль)
   
   **5.3. Интеграция в условные форматы:**
   - Добавить подсказку рядом с полем "Telegram Chat ID" в шаблонах
   - Использовать тот же компонент `ChatIdHelp`
   
   **5.4. Улучшение валидации:**
   - При ошибке валидации Chat ID показывать ссылку "Как получить Chat ID?"
   - Ссылка открывает инструкцию

6. **Дополнительные улучшения:**

   **6.1. Кнопки быстрого доступа:**
   - Кнопка "Открыть @getmyid_bot" (открывает Telegram с ботом)
   - Использовать `tg://resolve?domain=getmyid_bot` для открытия в Telegram
   - Кнопка должна быть визуально выделена и удобно расположена рядом с инструкцией
   
   **6.2. Проверка формата в реальном времени:**
   - Показывать подсказку о формате при вводе
   - Визуальная индикация правильности формата (зеленая галочка)
   
   **6.3. Подсказка для Bot Token:**
   - Добавить информационную подсказку рядом с полем "Bot Token"
   - **Важное предупреждение:** В подсказке должно быть указано, что этого бота нужно использовать **только для сигналов с этого сайта**
   - Формулировка: "⚠️ **Важно:** Создайте отдельного бота через @BotFather и используйте его только для получения сигналов с этого сайта. Не используйте этого бота для других целей."
   - Визуальное оформление: предупреждающий стиль (желтый/оранжевый фон, иконка предупреждения)
   - Можно добавить раскрывающийся блок с подробным объяснением, почему это важно
   
   **6.4. FAQ секция:**
   - Добавить часто задаваемые вопросы:
     - "Где найти Chat ID в сообщении бота @getmyid_bot?"
       - Ответ: Найдите строку "Current chat ID" в сообщении бота (для групп) или "Your ID" (для личных чатов)
     - "Почему Chat ID отрицательный?"
       - Ответ: Отрицательные числа используются для групп и каналов, положительные — для личных чатов
     - "Какой ID использовать: Your ID или Current chat ID?"
       - Ответ: Для групп и каналов используйте "Current chat ID", для личных чатов — "Your ID"
     - "Можно ли использовать один Chat ID для нескольких аккаунтов?"
       - Ответ: Да, один Chat ID (группы) можно использовать для всех аккаунтов
     - "Бот не отвечает после добавления в группу"
       - Ответ: Убедитесь, что бот добавлен корректно и имеет права на отправку сообщений
     - "Бот не отправил сообщение после добавления в канал"
       - Ответ: В каналах бот должен иметь права администратора или право на отправку сообщений. Перейдите в настройки канала → "Администраторы" → найдите @getmyid_bot → дайте ему права администратора или включите право "Отправлять сообщения"
     - "Как дать боту права в канале?"
       - Ответ: Настройки канала → "Администраторы" → найдите бота → включите права администратора или право "Отправлять сообщения"
     - "Можно ли использовать одного бота для нескольких сайтов?"
       - Ответ: Рекомендуется создать отдельного бота для каждого сайта/сервиса для безопасности и удобства управления

7. **Структура файлов:**

   ```
   WEB/
     components/
       ChatIdHelp.tsx          # Основной компонент с инструкцией
     app/(dashboard)/components/
       DashboardShell.tsx      # Интеграция в настройки Telegram
   ```

8. **Порядок реализации:**

   1. **Создать компонент `ChatIdHelp.tsx`:**
      - Реализовать раскрывающийся блок с инструкцией
      - Добавить пошаговые инструкции для всех способов
      - Добавить визуальное оформление
      - Добавить кнопки быстрого доступа к ботам
   
   2. **Интегрировать в DashboardShell.tsx:**
      - Добавить компонент рядом с полем "Chat ID"
      - Улучшить существующую подсказку
      - Добавить ссылку на инструкцию при ошибке валидации
      - Добавить подсказку с предупреждением для поля "Bot Token" о том, что бота нужно использовать только для сигналов с этого сайта
   
   3. **Добавить в условные форматы:**
      - Разместить подсказку рядом с полем Chat ID в шаблонах
      - Использовать компактную версию компонента
   
   4. **Улучшить валидацию:**
      - Добавить более информативные сообщения об ошибках
      - Связать ошибки с инструкцией
   
   5. **Тестирование:**
      - Проверить работу на разных устройствах
      - Проверить открытие ссылок на ботов
      - Проверить валидацию с разными форматами Chat ID
      - Проверить работу в модальном окне (если реализовано)

**Приоритет:** Низкий

**Статус:** ✅ Выполнено

**Что реализовано:**
- ✅ Создан компонент `ChatIdHelp.tsx` с полной инструкцией по получению Chat ID
- ✅ Интегрирован в основную форму настроек Telegram (рядом с полем Chat ID)
- ✅ Добавлен в условные форматы сообщений (компактная версия)
- ✅ Улучшена валидация Chat ID с ссылкой на инструкцию при ошибке
- ✅ Добавлена подсказка для Bot Token с предупреждением о важности использования отдельного бота
- ✅ Реализован раскрывающийся блок с пошаговой инструкцией для групп/каналов и личных чатов
- ✅ Добавлена информация о правах бота в каналах
- ✅ Добавлены кнопки быстрого доступа к ботам (@getmyid_bot и @BotFather)
- ✅ Визуальное оформление соответствует стилю приложения
- ✅ Поддержка двух вариантов: `default` (полная инструкция) и `compact` (компактная версия)

**Что не реализовано (не критично):**
- ⏳ FAQ секция как отдельный блок (информация включена в инструкцию)
- ⏳ Визуальная индикация правильности формата в реальном времени (зеленая галочка) - валидация работает при потере фокуса

---

### 4. Улучшение подсказок (tooltips)
**Проблема:** Подсказки возле названий блоков повторяют описания блоков.

**Задача:** 
- Сделать подсказки более подробными и информативными
- Отличать подсказки от основных описаний блоков
- При наведении мышкой показывать дополнительную полезную информацию

**Приоритет:** Низкий

---

## Технические улучшения

### 5. Настройка перезапуска после деплоя
**Задача:** Настроить систему так, чтобы `main.py` не перезапускался на сервере после получения обновлений с GitHub, а остальные компоненты перезапускались.

**Приоритет:** Средний

---

### 6. Добавление принудительного переподключения WebSocket каждые 23 часа
**Проблема:** В настоящее время WebSocket соединения переподключаются только при ошибках или разрыве соединения. Долгие соединения могут терять актуальность данных или иметь проблемы с таймаутами на стороне биржи.

**Задача:** 
- Добавить отслеживание времени начала каждого WebSocket соединения
- Реализовать принудительное переподключение всех WebSocket соединений раз в 23 часа (82800 секунд)
- Применить это для всех бирж: Gate, Binance, Bitget, Bybit, Hyperliquid
- Обеспечить плавное переподключение без потери данных
- **Важно:** Плановые переподключения раз в 23 часа не должны учитываться в счетчике переподключений (reconnects), так как это плановое техническое обслуживание, а не ошибка

**Приоритет:** Средний

---

## Контент и тексты

### 7. Проверка и исправление текстовых надписей
**Задача:** 
- Проверить все текстовые надписи в проекте
- Внести логические изменения и исправления
- Улучшить формулировки для лучшего понимания

**Приоритет:** Низкий

---

## Статус выполнения

- [x] 0. Нормализация отображения названий монет
- [x] 1. Исправление работы глобальных настроек на дополнительных парах
- [x] 2. Улучшение блока "Условные форматы сообщений"
- [x] 3. Подсказка для получения Chat ID
- [ ] 4. Улучшение подсказок (tooltips)
- [ ] 5. Настройка перезапуска после деплоя
- [ ] 6. Добавление принудительного переподключения WebSocket каждые 23 часа
- [ ] 7. Проверка и исправление текстовых надписей

---

## Тестовые сценарии для проверки

### Проверка работы условных форматов сообщений на дополнительных парах

**Сценарий:**

1. **Настройки профиля:**
   - На Binance Spot для пары TRY настроены фильтры:
     - Дельта: 2%
     - Объем: 500000 USDT
     - Тень: 50%

2. **Условный формат сообщения:**
   - Серия стрел: 3 шт за 300 секунд
   - Дельта: от 0.4% до бесконечности
   - Объем: 65000 USDT

3. **Событие:**
   - На паре TRY-ETH происходит 5 палок подряд в течение 5 минут
   - Дельта: 0.5%
   - Объем: 50000 USDT
   - Тень свечи: больше 50%

**Вопрос для проверки:**
Должен ли дополнительный условный формат сообщения поймать детект в такой ситуации на паре TRY-ETH?

**Ожидаемое поведение:**
- Условный формат сообщения должен сработать, если:
  - Количество стрел (5) >= требуемого (3)
  - Временной интервал (5 минут = 300 секунд) <= требуемого (300 секунд)
  - Дельта (0.5%) >= минимальной (0.4%)
  - Объем (50000 USDT) < требуемого (65000 USDT) — **НЕ соответствует условию**
- Если объем не соответствует условию, детект не должен сработать

**Требуется проверить:**
- Корректность работы условных форматов сообщений на дополнительных парах (TRY-ETH)
- Применение условий по объему, дельте, количеству стрел и времени
- Логику проверки всех условий условного формата сообщения

---

*Последнее обновление: 2024*

