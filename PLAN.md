# Пошаговый план деплоя вашего сайта на Vultr сервер

## 📋 Обзор

Этот план поможет вам:
1. Создать публичный доступ к вашему сайту
2. Настроить автоматическую синхронизацию кода с вашего ПК на сервер
3. Запустить всё на сервере 24/7

---

## Шаг 0: Установка Git на ваш ПК (ОБЯЗАТЕЛЬНО!)

⚠️ **ВАЖНО:** Если команды `git` не работают на вашем ПК, сначала нужно установить Git!

### 0.1. Скачивание Git для Windows

1. Откройте браузер на вашем ПК
2. Перейдите на сайт: https://git-scm.com/download/win
3. Скачайте установщик (автоматически определит вашу систему)
4. Запустите скачанный файл (например: `Git-2.43.0-64-bit.exe`)

### 0.2. Установка Git

1. Нажмите **Next** (Далее) в первом окне
2. Выберите место установки (оставьте по умолчанию) → **Next**
3. Выберите компоненты (оставьте по умолчанию) → **Next**
4. Выберите имя папки в меню Пуск (оставьте по умолчанию) → **Next**
5. Выберите редактор (оставьте по умолчанию или выберите "Notepad++" если установлен) → **Next**
6. Выберите название ветки (оставьте "Let Git decide") → **Next**
7. Выберите способ использования PATH:
   - ✅ Рекомендуется: **"Git from the command line and also from 3rd-party software"**
   - Нажмите **Next**
8. Выберите HTTPS (оставьте по умолчанию) → **Next**
9. Выберите формат окончаний строк (оставьте по умолчанию) → **Next**
10. Выберите эмулятор терминала (оставьте по умолчанию) → **Next**
11. Выберите поведение по умолчанию (оставьте по умолчанию) → **Next**
12. Выберите дополнительные опции (оставьте по умолчанию) → **Next**
13. Нажмите **Install** (Установить)
14. Дождитесь завершения установки
15. Нажмите **Finish** (Готово)

### 0.3. Проверка установки Git

1. **Закройте** командную строку (cmd.exe), если она открыта
2. Откройте **новую** командную строку:
   - Нажмите клавишу Windows
   - Введите: `cmd`
   - Нажмите Enter
3. Введите команду:

```bash
git --version
```

4. Должно показать версию (например: `git version 2.43.0`)

✅ **Если видите версию - Git установлен правильно!**
❌ **Если ошибка - перезагрузите компьютер и попробуйте снова**

### 0.4. Настройка Git (имя и email)

После установки Git нужно настроить ваше имя и email. В командной строке выполните:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

**Замените на ваши данные!**

Например:
```bash
git config --global user.name "Vlad"
git config --global user.email "vlad@example.com"
```

**Готово!** Теперь можно переходить к Шагу 1.

---

## Шаг 1: Подготовка GitHub репозитория

### 1.1. Создание аккаунта на GitHub (если еще нет)

- Откройте сайт: https://github.com
- Нажмите кнопку **Sign up** (Регистрация)
- Создайте аккаунт (бесплатно)
- Подтвердите email

### 1.2. Создание нового репозитория

1. Зайдите на https://github.com
2. Нажмите зелёную кнопку **New** (Создать) или **+** → **New repository**
3. Заполните форму:
   - **Repository name**: например `crypto-spikes-detector` (любое имя)
   - **Description**: описание проекта (необязательно)
   - **Public** или **Private** - выберите сами
   - ⚠️ **ВАЖНО**: НЕ ставьте галочки на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - Нажмите **Create repository**

### 1.3. Загрузка кода в GitHub

**Вариант А: Автоматически (рекомендуется)**

1. **ВАЖНО:** Убедитесь, что Git установлен (см. Шаг 0 выше)
2. На вашем ПК откройте папку с проектом: `C:\Users\Vlad\onlyWS`
3. Найдите файл `setup-git.bat`
4. Дважды кликните на него
5. Скрипт спросит URL вашего репозитория - вставьте его (например: `https://github.com/ваш-логин/название-репозитория.git`)
6. Нажмите Enter
7. Если попросит логин и пароль - используйте:
   - Логин: ваш логин GitHub
   - Пароль: Personal Access Token (см. пункт 1.4 ниже)

**Вариант Б: Вручную**

1. **ВАЖНО:** Убедитесь, что Git установлен (см. Шаг 0 выше)
2. Откройте **новую** командную строку:
   - Нажмите клавишу Windows
   - Введите: `cmd`
   - Нажмите Enter
3. Перейдите в папку проекта:

```bash
cd C:\Users\Vlad\onlyWS
```

4. Выполните команды по порядку:

```bash
git init
```

Дождитесь завершения, затем:

```bash
git add .
```

Дождитесь завершения, затем:

```bash
git commit -m "Initial commit"
```

Дождитесь завершения, затем:

```bash
git branch -M main
```

5. Добавьте ваш репозиторий (замените URL на ваш):

```bash
git remote add origin https://github.com/ВАШ_ЛОГИН/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
```

Например:
```bash
git remote add origin https://github.com/Vltmsk/MAIN.git
```

6. Отправьте код в GitHub:

```bash
git push -u origin main
```

7. Если попросит логин:
   - **Username**: введите ваш логин GitHub (например: `Vltmsk`)
   - **Password**: введите Personal Access Token (см. пункт 1.4 ниже)
   - ⚠️ **НЕ вводите пароль от GitHub! Используйте токен!**

**Если ошибка:** Убедитесь, что:
- Git установлен правильно (см. Шаг 0)
- Вы используете **Personal Access Token**, а не пароль
- URL репозитория правильный

### 1.4. Создание Personal Access Token (для пароля GitHub)

GitHub требует использовать токен вместо пароля:

1. Зайдите на https://github.com → настройки (Settings)
2. В левом меню выберите **Developer settings**
3. Выберите **Personal access tokens** → **Tokens (classic)**
4. Нажмите **Generate new token** → **Generate new token (classic)**
5. Название: `Мой токен` (любое)
6. Срок действия: `90 days` или `No expiration`
7. Отметьте галочку **repo** (доступ к репозиториям)
8. Нажмите **Generate token**
9. ⚠️ **СОХРАНИТЕ ТОКЕН** - его больше не покажут! Скопируйте его в блокнот

**Этот токен используйте как пароль при git push**

---

## Шаг 2: Подключение к серверу Vultr

### 2.1. Узнайте IP адрес вашего сервера

1. Зайдите в панель управления Vultr: https://my.vultr.com
2. Найдите ваш сервер в списке
3. Скопируйте **IP Address** (например: `123.456.789.012`)

### 2.2. Узнайте пароль администратора

1. В панели Vultr нажмите на ваш сервер
2. Найдите раздел **Server Details**
3. Нажмите **View Console** или найдите кнопку **Password**
4. Скопируйте пароль администратора Windows
5. Сохраните его в безопасном месте

### 2.3. Подключение через Remote Desktop (RDP)

1. На вашем ПК нажмите клавишу **Windows**
2. Введите: **Remote Desktop Connection** (Подключение к удалённому рабочему столу)
3. Откройте приложение
4. В поле **Компьютер** введите IP адрес сервера (например: `123.456.789.012`)
5. Нажмите **Подключить**
6. Введите:
   - Имя пользователя: обычно `Administrator` или имя, которое вы создали
   - Пароль: пароль из пункта 2.2
7. Нажмите **ОК**
8. При предупреждении о сертификате нажмите **Да**

Теперь вы подключены к серверу!

---

## Шаг 3: Установка программ на сервере

На сервере (в окне Remote Desktop) нужно установить программы.

### 3.1. Установка Python

1. Откройте браузер на сервере (Edge или Chrome)
2. Перейдите на: https://www.python.org/downloads/
3. Нажмите кнопку **Download Python** (самая большая)
4. Запустите скачанный файл
5. ⚠️ **ВАЖНО**: Поставьте галочку **"Add Python to PATH"**
6. Нажмите **Install Now**
7. Дождитесь завершения установки
8. Нажмите **Close**

**Проверка:**
- Откройте PowerShell на сервере
- Введите: `python --version`
- Должно показать версию (например: `Python 3.11.5`)

### 3.2. Установка Node.js

1. Перейдите на: https://nodejs.org/
2. Нажмите кнопку **LTS** (зелёная кнопка Download)
3. Запустите скачанный файл
4. Всё время нажимайте **Next** с настройками по умолчанию
5. Нажмите **Finish** в конце

**Проверка:**
- В PowerShell введите: `node --version`
- Должно показать версию (например: `v20.10.0`)

### 3.3. Установка Git

1. Перейдите на: https://git-scm.com/download/win
2. Скачайте установщик
3. Запустите установщик
4. Всё время нажимайте **Next** с настройками по умолчанию
5. Нажмите **Finish** в конце

**Проверка:**
- В PowerShell введите: `git --version`
- Должно показать версию (например: `git version 2.42.0`)

---

## Шаг 4: Клонирование проекта на сервер

1. На сервере откройте PowerShell
2. Перейдите в корень диска C:

```powershell
cd C:\
```

3. Клонируйте ваш репозиторий:

```powershell
git clone https://github.com/ВАШ_ЛОГИН/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git onlyWS
```

Замените `ВАШ_ЛОГИН` и `НАЗВАНИЕ_РЕПОЗИТОРИЯ` на ваши данные.

4. Если попросит логин и пароль:
   - Логин: ваш логин GitHub
   - Пароль: Personal Access Token из шага 1.4

5. Дождитесь завершения загрузки

**Проверка:**
```powershell
cd onlyWS
dir
```

Должны увидеть файлы проекта (main.py, api_server.py и т.д.)

---

## Шаг 5: Установка зависимостей на сервере

1. Убедитесь, что вы в папке проекта:

```powershell
cd C:\onlyWS
```

2. Установите Python зависимости:

```powershell
pip install -r requirements.txt
```

Дождитесь завершения (может занять 2-5 минут)

3. Установите Node.js зависимости и соберите Next.js:

```powershell
cd WEB
npm install
```

Дождитесь завершения (может занять 3-10 минут)

4. Соберите Next.js для production:

```powershell
npm run build
```

Дождитесь завершения (может занять 1-2 минуты)

5. Вернитесь в корневую папку:

```powershell
cd ..
```

---

## Шаг 6: Настройка переменных окружения (не сделал)

1. На сервере в папке `C:\onlyWS` создайте файл `.env`
2. Откройте Блокнот
3. Создайте новый файл
4. Введите:

```
DOMAIN=ваш-домен.com
```

**Замените `ваш-домен.com` на ваш реальный домен!**

5. Сохраните файл как `.env` в папку `C:\onlyWS`(сделал)
   - ⚠️ В блокноте при сохранении выберите "Все файлы" вместо "Текстовые документы"
   - Полный путь: `C:\onlyWS\.env`

---

## Шаг 7: Настройка DNS и домена(сделал)

### 7.1. Узнайте IP адрес вашего сервера

- Из шага 2.1 (например: `123.456.789.012`)

### 7.2. Настройте DNS у регистратора домена

1. Зайдите в панель управления вашего регистратора домена (где вы покупали домен)
2. Найдите раздел **DNS** или **Управление DNS**
3. Найдите **A-запись** или возможность добавить новую запись
4. Добавьте A-запись:
   - **Имя/Хост**: `@` или оставьте пустым (это означает ваш основной домен)
   - **Тип**: `A`
   - **Значение/IP**: вставьте IP адрес вашего сервера (из шага 7.1)
   - **TTL**: `3600` или оставьте по умолчанию
5. Сохраните изменения

6. (Опционально) Добавьте CNAME для www:
   - **Имя/Хост**: `www`
   - **Тип**: `CNAME`
   - **Значение**: ваш домен (например: `your-domain.com`)
   - Сохраните

**Важно:** Изменения DNS распространяются от 5 минут до 24 часов. Подождите.

**Проверка DNS:** Зайдите на https://dnschecker.org/ и проверьте, что A-запись указывает на ваш IP.

---

## Шаг 8: Настройка файрвола Windows (Windows Firewall Configuration) (сделал)

1. На сервере откройте **Control Panel** (Панель управления):
   - Нажмите клавишу **Windows**
   - Введите: `Control Panel`
   - Нажмите Enter
2. Перейдите в **System and Security** → **Windows Defender Firewall**
   - Или найдите **Windows Defender Firewall** в поиске
3. Нажмите **Advanced settings** (Дополнительные параметры) в левом меню
4. Выберите **Inbound Rules** (Правила для входящих подключений) в левом меню
5. Нажмите **New Rule...** (Создать правило...) в правой панели
6. Выберите **Port** (Для порта) → **Next** (Далее)
7. Выберите **TCP** и **Specific local ports** (Определенные локальные порты)
8. В поле введите: `80` → **Next** (Далее)
9. Выберите **Allow the connection** (Разрешить подключение) → **Next** (Далее)
10. Отметьте все три галочки:
    - ✅ **Domain** (Домен)
    - ✅ **Private** (Частная)
    - ✅ **Public** (Публичная)
    → **Next** (Далее)
11. В поле **Name** (Имя) введите: `HTTP (Port 80)` → **Finish** (Готово)

Повторите для порта **443** (HTTPS):
- Шаги 5-10 такие же
- В пункте 8 введите: `443`
- В пункте 11 имя: `HTTPS (Port 443)`

**Альтернативный способ (через PowerShell от имени администратора):**

Если удобнее через командную строку, выполните:

```powershell
# Правило для порта 80 (HTTP)
New-NetFirewallRule -DisplayName "HTTP (Port 80)" -Direction Inbound -LocalPort 80 -Protocol TCP -Action Allow

# Правило для порта 443 (HTTPS)
New-NetFirewallRule -DisplayName "HTTPS (Port 443)" -Direction Inbound -LocalPort 443 -Protocol TCP -Action Allow
```

**Проверка правил:**
- В окне **Inbound Rules** должны появиться два новых правила:
  - `HTTP (Port 80)`
  - `HTTPS (Port 443)`
- Оба должны иметь статус **Enabled** (Включено)

---

## Шаг 9: Установка NSSM (для служб Windows)(сделал)

### 9.1. Скачивание и установка NSSM

1. Перейдите на: https://nssm.cc/download
2. Скачайте архив (например: `nssm-2.24.zip`)
3. Распакуйте архив (правой кнопкой → Извлечь все)
4. Переименуйте папку в `nssm` и переместите в `C:\nssm`
   - Полный путь должен быть: `C:\nssm\win64\nssm.exe`

### 9.2. Настройка PowerShell

1. Откройте PowerShell на сервере **от имени администратора**:
   - Правой кнопкой на PowerShell → **Запуск от имени администратора**

2. Найдите пути к Python и Node.js:

```powershell
where python
where node
```

3. Скопируйте пути (например: `C:\Python\python.exe` и `C:\Program Files\nodejs\node.exe`)

### 9.3. Создание службы для main.py (сбор данных с бирж)

В PowerShell (от имени администратора):

```powershell
cd C:\nssm\win64

.\nssm.exe install CryptoSpikesMain "C:\Python\python.exe" "C:\onlyWS\main.py"
.\nssm.exe set CryptoSpikesMain AppDirectory "C:\onlyWS"
.\nssm.exe set CryptoSpikesMain DisplayName "Crypto Spikes Main Service"
.\nssm.exe set CryptoSpikesMain Description "Сбор данных с криптобирж"
.\nssm.exe set CryptoSpikesMain Start SERVICE_AUTO_START
.\nssm.exe set CryptoSpikesMain AppStdout "C:\onlyWS\logs\main_service.log"
.\nssm.exe set CryptoSpikesMain AppStderr "C:\onlyWS\logs\main_service_error.log"
```

**⚠️ Замените `C:\Python\python.exe` на путь из пункта 9.2!**

### 9.4. Создание службы для API (FastAPI backend)(сделал)

```powershell
.\nssm.exe install CryptoSpikesAPI "C:\Python\python.exe" "-m" "uvicorn" "api_server:app" "--host" "0.0.0.0" "--port" "8001"
.\nssm.exe set CryptoSpikesAPI AppDirectory "C:\onlyWS"
.\nssm.exe set CryptoSpikesAPI DisplayName "Crypto Spikes API Service"
.\nssm.exe set CryptoSpikesAPI Description "FastAPI Backend"
.\nssm.exe set CryptoSpikesAPI Start SERVICE_AUTO_START
.\nssm.exe set CryptoSpikesAPI AppStdout "C:\onlyWS\logs\api_service.log"
.\nssm.exe set CryptoSpikesAPI AppStderr "C:\onlyWS\logs\api_service_error.log"
```

**⚠️ Замените `C:\Python\python.exe` на путь из пункта 9.2!**

### 9.5. Создание службы для Next.js (веб-интерфейс)(сделал)

```powershell
.\nssm.exe install CryptoSpikesWeb "C:\Program Files\nodejs\node.exe" "C:\onlyWS\WEB\node_modules\.bin\next" "start" "--port" "3000"
.\nssm.exe set CryptoSpikesWeb AppDirectory "C:\onlyWS\WEB"
.\nssm.exe set CryptoSpikesWeb DisplayName "Crypto Spikes Web Service"
.\nssm.exe set CryptoSpikesWeb Description "Next.js Frontend"
.\nssm.exe set CryptoSpikesWeb Start SERVICE_AUTO_START
.\nssm.exe set CryptoSpikesWeb AppStdout "C:\onlyWS\logs\web_service.log"
.\nssm.exe set CryptoSpikesWeb AppStderr "C:\onlyWS\logs\web_service_error.log"
```

**⚠️ Замените `C:\Program Files\nodejs\node.exe` на путь из пункта 9.2!**

### 9.6. Запуск служб(сделал, но не поолучилось)

```powershell
Start-Service CryptoSpikesMain
Start-Service CryptoSpikesAPI
Start-Service CryptoSpikesWeb
```

### 9.7. Проверка служб (проопустил)

```powershell
Get-Service CryptoSpikes*
```

Все три службы должны показывать статус **Running** (Работает)

---

## Шаг 10: Установка и настройка Nginx()

### 10.1. Скачивание и установка Nginx

1. Перейдите на: http://nginx.org/en/download.html
2. Скачайте **nginx/Windows** (например: `nginx-1.24.0.zip`)
3. Распакуйте архив в `C:\nginx`
   - Полный путь должен быть: `C:\nginx\nginx.exe`

### 10.2. Настройка Nginx

1. Откройте файл `C:\nginx\conf\nginx.conf` в Блокноте (правой кнопкой → Открыть с помощью → Блокнот)

2. Найдите строку:

```nginx
http {
```

3. Внутри блока `http { ... }` добавьте или замените на:

```nginx
http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # Upstream для Next.js
    upstream nextjs {
        server 127.0.0.1:3000;
    }

    # Upstream для FastAPI
    upstream api {
        server 127.0.0.1:8001;
    }

    server {
        listen       80;
        server_name  ваш-домен.com www.ваш-домен.com;

        # Для Next.js
        location / {
            proxy_pass http://nextjs;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Для API
        location /api {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
```

**⚠️ Замените `ваш-домен.com` на ваш реальный домен!**

4. Сохраните файл

### 10.3. Создание службы для Nginx

В PowerShell (от имени администратора):

```powershell
cd C:\nssm\win64
.\nssm.exe install Nginx "C:\nginx\nginx.exe"
.\nssm.exe set Nginx AppDirectory "C:\nginx"
.\nssm.exe set Nginx DisplayName "Nginx Web Server"
.\nssm.exe set Nginx Start SERVICE_AUTO_START
.\nssm.exe set Nginx AppStopMethodSkip 1
Start-Service Nginx
```

### 10.4. Проверка Nginx

```powershell
Get-Service Nginx
```

Статус должен быть **Running**

---

## Шаг 11: Настройка автоматической синхронизации

### 11.1. Настройка Git на сервере (если нужно)

В PowerShell:

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

### 11.2. Создание задачи в планировщике заданий

В PowerShell (от имени администратора):

```powershell
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-ExecutionPolicy Bypass -File C:\onlyWS\deploy.ps1"
$trigger = New-ScheduledTaskTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Minutes 5) -RepetitionDuration (New-TimeSpan -Days 365)
$principal = New-ScheduledTaskPrincipal -UserId "$env:USERDOMAIN\$env:USERNAME" -LogonType S4U -RunLevel Highest
Register-ScheduledTask -TaskName "AutoDeploySync" -Action $action -Trigger $trigger -Principal $principal -Description "Автоматическая синхронизация кода с GitHub"
```

**Готово!** Теперь каждые 5 минут скрипт будет проверять обновления в GitHub.

---

## Шаг 12: Проверка работы

### 12.1. Проверка служб

В PowerShell:

```powershell
Get-Service CryptoSpikes*
Get-Service Nginx
```

Все службы должны быть **Running**

### 12.2. Проверка логов

**Если сервис запущен через Windows Service (nssm):**

```powershell
# Просмотр логов в реальном времени
Get-Content C:\onlyWS\logs\main_service.log -Wait
# Нажмите Ctrl+C чтобы остановить
```

**Если приложение запущено напрямую (не как сервис):**

```powershell
# Просмотр логов в реальном времени
Get-Content C:\onlyWS\logs\app.log -Wait
# Нажмите Ctrl+C чтобы остановить
```

**Проверка наличия файлов логов:**

```powershell
# Проверка всех доступных логов
Get-ChildItem C:\onlyWS\logs\ | Select-Object Name, Length, LastWriteTime

# Просмотр последних 50 строк лога
Get-Content C:\onlyWS\logs\app.log -Tail 50
```

### 12.3. Проверка сайта

1. Откройте браузер на вашем ПК
2. Перейдите на ваш домен: `http://ваш-домен.com`
3. Сайт должен открыться!

4. Проверьте API: `http://ваш-домен.com/api/health`
   - Должен вернуться JSON с информацией о статусе

---

## ✅ Готово! Ваш сайт работает!

---

## 📝 Что дальше: Работа с кодом

### Редактирование на вашем ПК:

1. Откройте проект на вашем ПК
2. Отредактируйте файлы в любом редакторе
3. Сохраните изменения

### Загрузка изменений на сервер:

1. Откройте PowerShell или командную строку в папке проекта
2. Выполните:

```bash
git add .
git commit -m "Описание изменений"
git push
```

3. Введите логин и пароль (Personal Access Token)

4. Через 5 минут изменения автоматически появятся на сервере!

---

## 🔧 Полезные команды для сервера

### Просмотр статуса служб:

```powershell
Get-Service CryptoSpikes*
```

### Остановка служб:

```powershell
Stop-Service CryptoSpikesMain
Stop-Service CryptoSpikesAPI
Stop-Service CryptoSpikesWeb
```

### Запуск служб:

```powershell
Start-Service CryptoSpikesMain
Start-Service CryptoSpikesAPI
Start-Service CryptoSpikesWeb
```

### Перезапуск служб:

```powershell
Restart-Service CryptoSpikesMain
Restart-Service CryptoSpikesAPI
Restart-Service CryptoSpikesWeb
Restart-Service Nginx
```

### Просмотр логов:

```powershell
# Логи main.py
Get-Content C:\onlyWS\logs\main_service.log -Wait

# Логи API
Get-Content C:\onlyWS\logs\api_service.log -Wait

# Логи Next.js
Get-Content C:\onlyWS\logs\web_service.log -Wait

# Логи автоматического деплоя
Get-Content C:\onlyWS\deploy.log -Wait
```

Нажмите `Ctrl+C` чтобы остановить просмотр логов.

### Ручное обновление кода (если нужно):

```powershell
cd C:\onlyWS
powershell.exe -ExecutionPolicy Bypass -File deploy.ps1
```

---

## ❓ Решение проблем

### Сайт не открывается:

1. Проверьте DNS: https://dnschecker.org/
   - Введите ваш домен и проверьте, что A-запись указывает на правильный IP
   - Если нет - подождите до 24 часов

2. Проверьте службы:
```powershell
Get-Service CryptoSpikes*
Get-Service Nginx
```

3. Проверьте логи:
```powershell
Get-Content C:\onlyWS\logs\web_service.log
Get-Content C:\onlyWS\logs\api_service.log
```

### Служба не запускается:

1. Проверьте логи ошибок:
```powershell
Get-Content C:\onlyWS\logs\main_service_error.log
```

2. Проверьте пути к Python и Node.js в настройках службы через NSSM:
```powershell
cd C:\nssm\win64
.\nssm.exe edit CryptoSpikesMain
```
Проверьте путь к Python в поле "Path"

### Git не работает:

1. Проверьте настройки Git:
```powershell
git config --global --list
```

2. Если нужен токен - создайте новый Personal Access Token (см. Шаг 1.4)

### Порт занят:

```powershell
# Проверить какие процессы используют порты
netstat -ano | findstr "3000"
netstat -ano | findstr "8001"
```

---

## 📞 Где получить помощь

Если что-то не работает:

1. Проверьте логи в `C:\onlyWS\logs\`
2. Проверьте статус служб: `Get-Service CryptoSpikes*`
3. Проверьте лог деплоя: `C:\onlyWS\deploy.log`

---

## ✨ Успехов!

Ваш сайт теперь:
- ✅ Доступен публично по домену
- ✅ Работает 24/7 на сервере
- ✅ Автоматически обновляется при изменении кода

**Помните:** Редактируйте код на локальном ПК → `git push` → через 5 минут изменения на сервере!

