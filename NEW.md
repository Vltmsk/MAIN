# Telegram Chart Bot - Техническое задание

## 🎯 Глобальная концепция проекта

Telegram-бот для генерации и отправки тиковых графиков криптовалютных торговых пар по запросу пользователей. Бот поддерживает множественные биржи (Bitget, Gate, HTX, Bybit, Binance, Hyperliquid) и позволяет пользователям запрашивать графики для любой торговой пары на любой из поддерживаемых бирж.

### Основные возможности

1. **Интерактивные команды**: Пользователи могут запрашивать графики через команды бота
2. **Мульти-биржа**: Поддержка всех основных криптовалютных бирж
3. **Тиковые графики**: Построение графиков на основе реальных сделок (trades) с биржи
4. **Опциональный временной диапазон**: Пользователь может указать временной диапазон для графика (если не указан - используется дефолтное значение)
5. **Кэширование**: Оптимизация производительности через кэширование графиков
6. **Обработка ошибок**: Корректная обработка ошибок API бирж и пользовательских запросов

---

## 📐 Архитектура проекта

### Структура директорий

```
telegram_chart_bot/
├── bot/
│   ├── __init__.py
│   ├── bot.py                    # Основной файл бота (инициализация, запуск)
│   └── handlers/
│       ├── __init__.py
│       ├── commands.py           # Обработчики команд (/start, /help, /list_exchanges)
│       ├── chart_handler.py      # Обработчик запросов графиков
│       └── error_handler.py      # Обработка ошибок пользовательских запросов
│
├── chart/
│   ├── __init__.py
│   ├── generator.py              # Основной генератор графиков (изолированный модуль)
│   ├── data_fetcher.py           # Абстракция для получения данных с биржи
│   ├── types.py                  # Типы данных (Tick, ChartParams, ChartConfig)
│   ├── cache.py                  # Кэширование графиков
│   └── renderer.py               # Рендеринг графиков (matplotlib)
│
├── exchanges/
│   ├── __init__.py
│   ├── base.py                   # Базовый класс/интерфейс для бирж
│   ├── bitget/
│   │   ├── __init__.py
│   │   ├── api.py                # REST API для получения trades с Bitget
│   │   ├── client.py             # HTTP клиент для Bitget
│   │   └── LIMITS.md             # Документация лимитов Bitget API
│   ├── gate/
│   │   ├── __init__.py
│   │   ├── api.py
│   │   ├── client.py
│   │   └── LIMITS.md
│   ├── htx/
│   │   ├── __init__.py
│   │   ├── api.py
│   │   ├── client.py
│   │   └── LIMITS.md
│   ├── bybit/
│   │   ├── __init__.py
│   │   ├── api.py
│   │   ├── client.py
│   │   └── LIMITS.md
│   ├── binance/
│   │   ├── __init__.py
│   │   ├── api.py
│   │   ├── client.py
│   │   └── LIMITS.md
│   └── hyperliquid/
│       ├── __init__.py
│       ├── api.py
│       ├── client.py
│       └── LIMITS.md
│
├── utils/
│   ├── __init__.py
│   ├── logger.py                 # Настройка логирования
│   ├── symbol_utils.py           # Утилиты для работы с символами (нормализация)
│   └── validators.py             # Валидация пользовательских запросов
│
├── config/
│   ├── __init__.py
│   └── settings.py               # Настройки бота (токены, лимиты, таймауты)
│
├── requirements.txt
├── .env.example
├── README.md
└── main.py                       # Точка входа приложения
```

---

## 🔧 Технические требования

### Зависимости

- **Python 3.10+**
- **python-telegram-bot** или **aiogram** (асинхронная библиотека для Telegram Bot API)
- **aiohttp** - для асинхронных HTTP запросов к биржам
- **matplotlib** - для генерации графиков
- **pydantic** - для валидации данных
- **certifi** - для SSL сертификатов
- **pytz** - для работы с временными зонами

### Основные модули

1. **Модуль генерации графиков** (`chart/`)
   - Изолированный, самодостаточный модуль
   - Абстракция получения данных через `DataFetcher`
   - Поддержка кэширования
   - Настраиваемые параметры рендеринга

2. **Модуль интеграции бирж** (`exchanges/`)
   - Единый интерфейс для всех бирж
   - Реализация для каждой биржи в отдельной директории
   - Документация лимитов в `LIMITS.md` для каждой биржи

3. **Модуль бота** (`bot/`)
   - Обработка команд пользователей
   - Парсинг запросов графиков
   - Отправка графиков пользователям

---

## 📊 Функциональные требования

### Команды бота

1. **/start** - Приветственное сообщение с инструкциями
2. **/help** - Справка по использованию бота
3. **/list_exchanges** - Список поддерживаемых бирж
4. **/chart** - Запрос графика (основная команда)

### Формат запроса графика

```
/chart <exchange> <market> <symbol> [--timeframe <range>]
```

**Примеры:**
- `/chart bitget spot BTCUSDT`
- `/chart binance futures ETHUSDT`
- `/chart gate spot SOLUSDT --timeframe 1h`
- `/chart bybit spot BTCUSDT --timeframe 30m`

**Параметры:**
- `exchange` - название биржи (bitget, gate, htx, bybit, binance, hyperliquid)
- `market` - тип рынка (spot, futures, linear)
- `symbol` - торговый символ (BTCUSDT, ETHUSDT и т.д.)
- `--timeframe` - временной диапазон (опционально): `5m`, `15m`, `30m`, `1h`, `4h`, `24h` (если не указан - используется дефолтное значение, например последний час)

### Обработка запросов

1. **Валидация запроса**: Проверка корректности параметров (биржа, рынок, символ, timeframe)
2. **Расчёт временного диапазона**: Если указан timeframe - вычисление start_time и end_time
3. **Получение данных**: Запрос сделок с биржи за указанный период через REST API
4. **Генерация графика**: Построение тикового графика на основе полученных сделок
5. **Кэширование**: Сохранение графика в кэш (если применимо)
6. **Отправка**: Отправка графика пользователю в Telegram

### Обработка ошибок

- Неверный формат команды → информативное сообщение об ошибке
- Неподдерживаемая биржа → список доступных бирж
- Неверный символ → предложение похожих символов
- Ошибка API биржи → сообщение об ошибке с деталями
- Таймаут запроса → сообщение о превышении времени ожидания

---

## 🏦 Интеграция бирж

### Порядок реализации

1. **Bitget** (первая биржа)
2. **Gate**
3. **HTX**
4. **Bybit**
5. **Binance**
6. **Hyperliquid**

### Требования к реализации биржи

Каждая биржа должна иметь:

1. **REST API клиент** (`exchanges/{exchange}/client.py`)
   - Асинхронные HTTP запросы
   - Обработка ошибок и таймаутов
   - Соблюдение rate limits

2. **API методы** (`exchanges/{exchange}/api.py`)
   - `fetch_trades(symbol: str, market: str, start_time: Optional[int] = None, end_time: Optional[int] = None) -> List[Trade]`
   - `fetch_symbols(market: str) -> List[str]` (опционально, для валидации)
   - Нормализация данных в единый формат

3. **Документация лимитов** (`exchanges/{exchange}/LIMITS.md`)
   - Официальные rate limits биржи
   - Лимиты на количество запросов
   - Рекомендации по использованию
   - Примеры запросов

### Базовый интерфейс биржи

```python
# exchanges/base.py
from abc import ABC, abstractmethod
from typing import List
from chart.types import Tick

class ExchangeBase(ABC):
    """Базовый класс для всех бирж"""
    
    @abstractmethod
    async def fetch_trades(
        self,
        symbol: str,
        market: str,
        start_time: Optional[int] = None,
        end_time: Optional[int] = None
    ) -> List[Tick]:
        """
        Получает сделки с биржи за указанный период
        
        Args:
            symbol: Торговый символ (например, BTCUSDT)
            market: Тип рынка (spot, futures, linear)
            start_time: Начало периода (timestamp в миллисекундах, опционально)
            end_time: Конец периода (timestamp в миллисекундах, опционально)
            Если не указаны - возвращаются последние сделки (до лимита API биржи)
            
        Returns:
            Список тиков (Tick)
        """
        pass
    
    @abstractmethod
    def get_name(self) -> str:
        """Возвращает название биржи"""
        pass
    
    @abstractmethod
    def get_supported_markets(self) -> List[str]:
        """Возвращает список поддерживаемых рынков"""
        pass
```

### Формат данных

Все биржи должны возвращать данные в едином формате:

```python
# chart/types.py
from typing import TypedDict, Literal

class Tick(TypedDict):
    """Тик (сделка) с биржи"""
    id: int              # ID сделки
    ts: int              # Timestamp в миллисекундах
    price: float         # Цена сделки
    qty: float           # Объём сделки
    side: Literal["buy", "sell"]  # Направление сделки
```

---

## 📈 Генерация графиков

### Модуль генератора (`chart/generator.py`)

```python
class ChartGenerator:
    """Генератор тиковых графиков"""
    
    def __init__(self, data_fetcher: DataFetcher, cache: Optional[Cache] = None):
        self.data_fetcher = data_fetcher
        self.cache = cache
    
    async def generate_chart(
        self,
        exchange: str,
        market: str,
        symbol: str,
        timeframe: Optional[str] = None,
        **kwargs
    ) -> Optional[bytes]:
        """
        Генерирует тиковый график
        
        Args:
            exchange: Название биржи
            market: Тип рынка (spot, futures, linear)
            symbol: Торговый символ
            timeframe: Временной диапазон (опционально, например "1h", "30m")
        
        Returns:
            Байты PNG изображения или None при ошибке
        """
        pass
```

### Параметры графика

- **Размер изображения**: 1920x1080 пикселей (Full HD)
- **DPI**: 100
- **Формат**: PNG
- **Цвета**: 
  - Зелёные точки для покупок (buy)
  - Красные точки для продаж (sell)
  - Серая линия для соединения тиков

### Заголовок графика

Формат заголовка:
```
{Exchange} {Market} | {Symbol} {Direction} {Delta}% ({StartPrice}→{EndPrice})
Volume: ${Volume} | {Additional Metrics}
```

---

## 📝 Документация лимитов бирж

Для каждой биржи создаётся отдельный файл `LIMITS.md` в директории биржи.

### Структура документации

```markdown
# {Exchange} API Limits

## Rate Limits

### REST API
- **Weight limit**: X requests per Y seconds
- **IP limit**: X requests per minute
- **Endpoint specific**: ...

### Trades Endpoint
- **URL**: ...
- **Method**: GET
- **Limit**: X requests per Y seconds
- **Max trades per request**: 1000

## Recommendations

- Использовать задержки между запросами: X секунд
- Максимальное количество параллельных запросов: X
- При запросе по временному диапазону учитывать лимиты биржи на количество возвращаемых сделок

## Examples

```python
# Пример запроса
async with aiohttp.ClientSession() as session:
    url = "..."
    params = {"symbol": "BTCUSDT", "limit": 1000}
    async with session.get(url, params=params) as response:
        trades = await response.json()
```

## Official Documentation

- [API Documentation](...)
- [Rate Limits](...)
```

---

## 🔄 Кэширование

### Стратегия кэширования

- **Ключ кэша**: `{exchange}_{market}_{symbol}_{timestamp}`
- **TTL**: 10 минут (600 секунд)
- **Хранение**: В памяти (словарь) или Redis (опционально)

### Логика кэша

1. При запросе графика проверяется кэш
2. Если график в кэше и не устарел → возвращается из кэша
3. Если графика нет или устарел → генерируется новый
4. Новый график сохраняется в кэш

---

## 🚀 Запуск и конфигурация

### Переменные окружения

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Настройки
LOG_LEVEL=INFO
CACHE_TTL=600
CHART_DEFAULT_TIMEFRAME=1h  # Дефолтный временной диапазон, если не указан пользователем

# Таймауты
API_TIMEOUT=10
REQUEST_TIMEOUT=30
```

### Конфигурационный файл

```python
# config/settings.py
from dataclasses import dataclass
from typing import Dict, List

@dataclass
class BotConfig:
    telegram_token: str
    log_level: str = "INFO"
    cache_ttl: int = 600
    default_timeframe: str = "1h"  # Дефолтный временной диапазон
    api_timeout: int = 10
    supported_exchanges: List[str] = None
    
    def __post_init__(self):
        if self.supported_exchanges is None:
            self.supported_exchanges = [
                "bitget", "gate", "htx", "bybit", "binance", "hyperliquid"
            ]
```

---

## 📋 Чек-лист реализации

### Этап 1: Основа проекта
- [ ] Создать структуру директорий
- [ ] Настроить зависимости (requirements.txt)
- [ ] Создать конфигурацию (.env, config/settings.py)
- [ ] Настроить логирование
- [ ] Создать базовый класс для бирж

### Этап 2: Модуль генерации графиков
- [ ] Реализовать `ChartGenerator`
- [ ] Реализовать `DataFetcher` (абстракция)
- [ ] Реализовать рендеринг графиков (matplotlib)
- [ ] Реализовать кэширование
- [ ] Создать типы данных

### Этап 3: Интеграция Bitget (первая биржа)
- [ ] Создать REST API клиент для Bitget
- [ ] Реализовать метод `fetch_trades`
- [ ] Нормализовать данные в формат `Tick`
- [ ] Создать `LIMITS.md` с документацией лимитов Bitget
- [ ] Протестировать получение данных
- [ ] Протестировать генерацию графика

### Этап 4: Telegram Bot
- [ ] Настроить бота (python-telegram-bot/aiogram)
- [ ] Реализовать команды (/start, /help, /list_exchanges)
- [ ] Реализовать обработчик `/chart`
- [ ] Реализовать парсинг параметров команды
- [ ] Реализовать валидацию запросов
- [ ] Реализовать отправку графиков
- [ ] Реализовать обработку ошибок

### Этап 5: Интеграция остальных бирж
- [ ] Gate
- [ ] HTX
- [ ] Bybit
- [ ] Binance
- [ ] Hyperliquid

Для каждой биржи:
- [ ] REST API клиент
- [ ] Метод `fetch_trades`
- [ ] Нормализация данных
- [ ] `LIMITS.md` документация
- [ ] Тестирование

### Этап 6: Оптимизация и улучшения
- [ ] Оптимизация производительности
- [ ] Улучшение обработки ошибок
- [ ] Добавление метрик и мониторинга
- [ ] Документация пользователя
- [ ] Тестирование всех бирж

---

## 🎨 Примеры использования

### Пример 1: Запрос графика Bitget Spot

```
Пользователь: /chart bitget spot BTCUSDT

Бот:
1. Валидирует запрос
2. Получает последние 1000 сделок с Bitget Spot для BTCUSDT
3. Генерирует тиковый график
4. Отправляет график пользователю
```

### Пример 2: Запрос с временным диапазоном

```
Пользователь: /chart binance futures ETHUSDT --timeframe 1h

Бот:
1. Валидирует запрос
2. Определяет временной диапазон (последний час)
3. Получает сделки за указанный период с Binance Futures для ETHUSDT
4. Генерирует график
5. Отправляет график пользователю
```

### Пример 3: Обработка ошибки

```
Пользователь: /chart unknown spot BTCUSDT

Бот: 
❌ Неподдерживаемая биржа: unknown
📋 Доступные биржи: bitget, gate, htx, bybit, binance, hyperliquid
```

---

## 🔒 Безопасность и лимиты

### Защита от злоупотреблений

- **Rate limiting**: Ограничение количества запросов от одного пользователя
- **Валидация**: Строгая валидация всех пользовательских запросов
- **Таймауты**: Ограничение времени выполнения запросов
- **Обработка ошибок**: Корректная обработка всех типов ошибок

### Соблюдение лимитов бирж

- Каждая биржа имеет свои rate limits
- Реализация задержек между запросами
- Использование семафоров для ограничения параллельных запросов
- Мониторинг количества запросов к каждой бирже

---

## 📚 Дополнительные требования

### Логирование

- Все важные события должны логироваться
- Уровни логирования: DEBUG, INFO, WARNING, ERROR
- Логирование запросов пользователей
- Логирование ошибок API бирж

### Тестирование

- Unit тесты для модуля генерации графиков
- Unit тесты для каждой биржи
- Интеграционные тесты для бота
- Тесты обработки ошибок

### Документация

- README.md с инструкциями по установке и запуску
- Документация API для разработчиков
- Примеры использования
- LIMITS.md для каждой биржи

---

## 🎯 Критерии успеха

1. ✅ Бот успешно обрабатывает запросы графиков от пользователей
2. ✅ Поддержка всех указанных бирж (Bitget, Gate, HTX, Bybit, Binance, Hyperliquid)
3. ✅ Корректная генерация тиковых графиков для всех бирж
4. ✅ Соблюдение rate limits всех бирж
5. ✅ Корректная обработка всех типов ошибок
6. ✅ Документация лимитов для каждой биржи
7. ✅ Производительность: генерация графика < 5 секунд
8. ✅ Надёжность: работа без падений при нормальной нагрузке

---

## 📞 Примечания

- Проект должен быть модульным и расширяемым
- Код должен быть чистым и документированным
- Приоритет на стабильность и надёжность
- Соблюдение best practices Python и асинхронного программирования

