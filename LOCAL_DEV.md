# 🚀 Локальная разработка веб-интерфейса

Это руководство поможет вам запустить веб-интерфейс локально для разработки и тестирования.

## 📋 Требования

1. **Node.js** (версия 18 или выше)
2. **Python** (для бэкенд API сервера)
3. **npm** (обычно устанавливается вместе с Node.js)

## 🔧 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

Просто запустите батник:
```
Start Local Dev.bat
```

Этот скрипт:
- Проверит и установит зависимости (если нужно)
- Запустит Next.js dev сервер на `http://localhost:3000`

### Вариант 2: Ручной запуск

1. **Установите зависимости** (если еще не установлены):
   ```bash
   cd WEB
   npm install
   ```

2. **Запустите dev сервер**:
   ```bash
   npm run dev
   ```

3. **Откройте браузер**:
   ```
   http://localhost:3000
   ```

## ⚙️ Настройки

### Переменные окружения

Создайте файл `WEB/.env.local` (если его еще нет):

```env
# URL бэкенд API сервера
BACKEND_URL=http://localhost:8001
```

**Важно:** Убедитесь, что API сервер (`api_server.py`) запущен на порту 8001!

### Запуск API сервера

В отдельном терминале запустите:

```bash
python api_server.py
```

Или используйте существующий батник для запуска всего проекта.

## 🎯 Что происходит при локальной разработке

1. **Next.js dev сервер** запускается на `http://localhost:3000`
   - Автоматическая перезагрузка при изменении файлов
   - Hot Module Replacement (HMR) - изменения видны сразу
   - Подробные ошибки в консоли браузера

2. **API запросы** проксируются через Next.js API routes к бэкенду на `http://localhost:8001`

3. **Изменения в коде** применяются мгновенно без перезагрузки страницы

## 📁 Структура для разработки

```
WEB/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница
│   ├── dashboard/         # Дашборд
│   ├── admin/             # Админ панель
│   ├── login/             # Страница входа
│   └── api/               # API proxy routes
├── components/            # React компоненты
├── .env.local             # Локальные настройки (не коммитится)
└── package.json           # Зависимости и скрипты
```

## 🔍 Полезные команды

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm run start

# Проверка кода линтером
npm run lint
```

## 🐛 Отладка

### Проблемы с подключением к API

1. Проверьте, что API сервер запущен:
   ```bash
   # Должен вернуть {"ok": true}
   curl http://localhost:8001/api/health
   ```

2. Проверьте переменную `BACKEND_URL` в `WEB/.env.local`

3. Проверьте CORS настройки в `api_server.py` (должен быть разрешен `http://localhost:3000`)

### Проблемы с зависимостями

Если возникают ошибки с модулями:

```bash
cd WEB
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портами

Если порт 3000 занят, Next.js автоматически предложит использовать следующий свободный порт (3001, 3002 и т.д.)

Или укажите порт явно:
```bash
npm run dev -- -p 3001
```

## 💡 Советы по разработке

1. **Используйте DevTools браузера** для отладки React компонентов
2. **Следите за консолью терминала** - там выводятся ошибки компиляции
3. **Используйте React DevTools** расширение для браузера
4. **Проверяйте Network tab** в DevTools для отладки API запросов

## 🚀 Готово к разработке!

Теперь вы можете:
- ✅ Редактировать файлы в `WEB/app/` и видеть изменения сразу
- ✅ Тестировать изменения локально перед пушем в git
- ✅ Отлаживать интерфейс с реальными данными от API
- ✅ Разрабатывать новые функции без влияния на production

## 📝 Важные замечания

- **Не коммитьте** `.env.local` в git (он уже в .gitignore)
- **Локальная разработка** не влияет на production сервер
- **Изменения** нужно пушить в git, чтобы они попали на сервер
- **API сервер** должен быть запущен для полноценной работы интерфейса

---

**Удачной разработки! 🎉**

