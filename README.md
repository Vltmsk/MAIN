# Unified Crypto Spikes Detector

Единая система для детекции криптовалютных ценовых движений (spikes) в реальном времени с поддержкой множественных бирж, веб-интерфейса и уведомлений через Telegram.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Основные возможности](#основные-возможности)
- [Архитектура](#архитектура)
- [Установка и настройка](#установка-и-настройка)
- [Быстрый старт](#быстрый-старт)
- [Поддерживаемые биржи](#поддерживаемые-биржи)
- [Веб-интерфейс](#веб-интерфейс)
- [API документация](#api-документация)
- [База данных](#база-данных)
- [Конфигурация](#конфигурация)
- [Уведомления Telegram](#уведомления-telegram)
- [Детекция стрел](#детекция-стрел)
- [Логирование и мониторинг](#логирование-и-мониторинг)
- [Производительность](#производительность)
- [Развёртывание](#развёртывание)
- [FAQ](#faq)

---

## 🎯 Описание проекта

**Unified Crypto Spikes Detector** — это полнофункциональная система для мониторинга криптовалютных рынков в реальном времени. Система:

- 📊 Собирает данные с нескольких бирж одновременно через WebSocket
- 🔍 Детектирует ценовые движения (spikes) на основе пользовательских фильтров
- 📱 Отправляет уведомления в Telegram при обнаружении стрел
- 🌐 Предоставляет современный веб-интерфейс для управления и мониторинга
- 💾 Хранит историю детектов в базе данных SQLite
- 📈 Отображает статистику и аналитику

---

## ✨ Основные возможности

### Детекция стрел
- ✅ Многопользовательская система с индивидуальными фильтрами
- ✅ Настройка порогов по дельте (%), объёму (USDT), тени свечи (%)
- ✅ Фильтры по биржам (Binance, Gate.io, Bitget, Bybit, Hyperliquid)
- ✅ Фильтры по рынкам (Spot, Futures/Linear)
- ✅ Условные форматы сообщений с диапазонами дельты
- ✅ Отдельные Telegram чаты для каждого условного шаблона
- ✅ Серийные детекты (фильтрация повторных стрел)

### Веб-интерфейс
- ✅ Современный UI на Next.js 16 с TypeScript
- ✅ Тёмная тема с glassmorphism эффектами
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Дашборд с статистикой и графиками
- ✅ Админ панель для управления пользователями
- ✅ Управление настройками бирж и фильтров
- ✅ Просмотр истории стрел с фильтрацией
- ✅ Управление логами ошибок

### API
- ✅ RESTful API на FastAPI
- ✅ Аутентификация пользователей
- ✅ Управление пользователями и настройками
- ✅ Получение стрел с фильтрацией
- ✅ Статистика и метрики
- ✅ Управление логами ошибок

### Мониторинг
- ✅ Система логирования с ротацией
- ✅ Мониторинг здоровья системы (heartbeat)
- ✅ Метрики использования ресурсов
- ✅ Статистика работы бирж
- ✅ Автоматическое переподключение при обрыве соединений

---

## 🏗️ Архитектура

### Структура проекта

```
onlyWS/
├── main.py                 # Главный модуль для запуска всех бирж
├── api_server.py           # FastAPI сервер для API и веб-интерфейса
├── config.py               # Конфигурация приложения
│
├── BD/                     # База данных
│   ├── database.py         # Модуль работы с SQLite
│   └── detected_alerts.db  # Файл базы данных (создаётся автоматически)
│
├── core/                   # Ядро системы
│   ├── candle_builder.py   # Построитель 1-секундных свечей
│   ├── spike_detector.py   # Детектор стрел на основе фильтров
│   ├── telegram_notifier.py # Отправка уведомлений в Telegram
│   ├── logger.py           # Система логирования
│   ├── metrics.py          # Метрики и статистика
│   ├── health_monitor.py   # Мониторинг здоровья системы
│   └── profiling.py        # Профилирование производительности
│
├── exchanges/              # Модули бирж
│   ├── binance/            # Binance
│   ├── gate/               # Gate.io
│   ├── bitget/             # Bitget
│   ├── bybit/              # Bybit
│   └── hyperliquid/        # Hyperliquid
│
└── WEB/                    # Веб-интерфейс (Next.js)
    ├── app/                # Next.js приложение
    ├── package.json        # Зависимости Node.js
    └── README.md           # Документация веб-интерфейса
```

### Компоненты системы

1. **WebSocket клиенты** (`exchanges/*/ws_handler.py`)
   - Подключение к биржам через WebSocket
   - Получение данных о сделках или свечах
   - Автоматическое переподключение

2. **Построитель свечей** (`core/candle_builder.py`)
   - Построение 1-секундных свечей из потоков сделок
   - Унифицированный формат свечей для всех бирж

3. **Детектор стрел** (`core/spike_detector.py`)
   - Анализ свечей на основе пользовательских фильтров
   - Определение ценовых движений (spikes)
   - Поддержка условных форматов сообщений

4. **Telegram нотификатор** (`core/telegram_notifier.py`)
   - Отправка уведомлений в Telegram
   - Поддержка кастомных emoji
   - Условные форматы сообщений

5. **База данных** (`BD/database.py`)
   - Хранение истории детектов
   - Управление пользователями и настройками
   - Логирование ошибок

6. **API сервер** (`api_server.py`)
   - RESTful API для веб-интерфейса
   - Аутентификация и управление пользователями
   - Получение данных и статистики

7. **Веб-интерфейс** (`WEB/`)
   - Next.js 16 с TypeScript
   - Современный UI с тёмной темой
   - Адаптивный дизайн

---

## 🚀 Установка и настройка

### Требования

- **Python** 3.8+
- **Node.js** 18+
- **npm** или **yarn**

### Установка зависимостей Python

```bash
# Основные зависимости
pip install aiohttp websockets fastapi uvicorn psutil

# Или установите все из requirements.txt (если есть)
pip install -r requirements.txt
```

### Установка зависимостей Node.js

```bash
cd WEB
npm install
```

---

## 🎬 Быстрый старт

### 1. Запуск основного модуля (сбор данных)

```bash
python main.py
```

Это запустит:
- WebSocket соединения ко всем включённым биржам
- Сбор данных и построение свечей
- Детекцию стрел для всех пользователей
- Отправку уведомлений в Telegram

### 2. Запуск API сервера

В отдельном терминале:

```bash
python api_server.py
```

API сервер будет доступен на `http://localhost:8001`

### 3. Запуск веб-интерфейса

В отдельном терминале:

```bash
cd WEB
npm run dev
```

Веб-интерфейс будет доступен на `http://localhost:3000`

### 4. Настройка бирж

Отредактируйте `config.py`:

```python
from config import config

# Включить/выключить биржи и рынки
config.exchanges.gate_spot = True       # Gate.io Spot
config.exchanges.gate_linear = True     # Gate.io Futures
config.exchanges.binance_spot = True    # Binance Spot
config.exchanges.binance_linear = True  # Binance Futures
config.exchanges.bitget_spot = True     # Bitget Spot
config.exchanges.bitget_linear = True   # Bitget Futures
config.exchanges.bybit_spot = True      # Bybit Spot
config.exchanges.bybit_linear = True    # Bybit Futures
config.exchanges.hyperliquid_spot = True # Hyperliquid Spot
config.exchanges.hyperliquid_linear = True # Hyperliquid Linear
```

---

## 🏪 Поддерживаемые биржи

### Binance
- **Spot**: ✅ Поддержка через Kline streams
- **Futures (Linear)**: ✅ Поддержка через Kline streams
- **Лимит**: 150 стримов на соединение
- **Документация**: [exchanges/binance/README_binance.md](exchanges/binance/README_binance.md)

### Gate.io
- **Spot**: ✅ Построение свечей из потоков сделок
- **Futures (Linear)**: ✅ Построение свечей из потоков сделок
- **Лимит**: 135 символов Spot, 100 Linear на соединение
- **Документация**: [exchanges/gate/README_GATE.md](exchanges/gate/README_GATE.md)

### Bitget
- **Spot**: ✅ Построение свечей из потоков сделок
- **Futures (Linear)**: ✅ Построение свечей из потоков сделок
- **Лимит**: 39 символов Spot, 100 Linear на соединение
- **Документация**: [exchanges/bitget/README_bitget.md](exchanges/bitget/README_bitget.md)

### Bybit
- **Spot**: ✅ Построение свечей из потоков сделок
- **Futures (Linear)**: ✅ Построение свечей из потоков сделок
- **Лимит**: 86 символов Spot, 100 Linear на соединение
- **Документация**: [exchanges/bybit/README_bybit.md](exchanges/bybit/README_bybit.md)

### Hyperliquid
- **Spot**: ✅ Построение свечей из потоков сделок
- **Futures (Linear)**: ✅ Построение свечей из потоков сделок
- **Документация**: См. код в `exchanges/hyperliquid/`

---

## 🌐 Веб-интерфейс

### Особенности

- **Современный дизайн**: Glassmorphism эффекты, градиенты, анимации
- **Тёмная тема**: Оптимизирован для комфортной работы
- **Адаптивность**: Работает на всех устройствах (десктоп, планшет, мобильный)
- **Интуитивный интерфейс**: Простое управление настройками

### Страницы

1. **Главная страница** (`/`)
   - Регистрация и вход
   - Белый список пользователей

2. **Дашборд** (`/dashboard`)
   - Личные настройки
   - Фильтры по биржам
   - Условные форматы сообщений
   - Статистика стрел

3. **Статистика** (`/stats`)
   - Графики детектов по дням
   - Топ 10 стрел по дельте и объёму
   - Топ 10 символов
   - Детали по монете

4. **Админ панель** (`/admin`)
   - Управление пользователями
   - Белый список логинов
   - Логи ошибок
   - Статистика бирж

### Технологии

- **Next.js 16** - React framework
- **TypeScript** - Типобезопасность
- **Tailwind CSS v4** - Стилизация
- **FastAPI** - Backend API

Подробная документация: [WEB/README.md](WEB/README.md)

---

## 📡 API документация

### Аутентификация

#### POST `/api/auth/register/{user}`
Регистрация нового пользователя.

**Body:**
```json
{
  "password": "string",
  "tg_token": "string (optional)",
  "chat_id": "string (optional)",
  "options_json": "{} (optional)"
}
```

#### POST `/api/auth/login/{user}`
Вход пользователя.

**Body:**
```json
{
  "password": "string",
  "timezone": "string (optional)",
  "timezone_offset_minutes": "number (optional)"
}
```

#### GET `/api/auth/whitelist`
Получить белый список логинов.

#### POST `/api/auth/whitelist`
Добавить логин в белый список.

**Body:**
```json
{
  "username": "string"
}
```

#### DELETE `/api/auth/whitelist/{username}`
Удалить логин из белого списка.

### Пользователи

#### GET `/api/users`
Получить всех пользователей.

#### GET `/api/users/{user}`
Получить пользователя по имени.

#### POST `/api/users/{user}/settings`
Создать или обновить настройки пользователя.

**Body:**
```json
{
  "tg_token": "string (optional)",
  "chat_id": "string (optional)",
  "options_json": "{}"
}
```

#### DELETE `/api/users/{user}`
Удалить пользователя (системные пользователи "Stats" и "Влад" защищены).

#### POST `/api/users/{user}/test`
Отправить тестовое сообщение в Telegram.

### Стрелы (Alerts/Spikes)

#### GET `/api/spikes`
Получить стрелы с фильтрацией.

**Query параметры:**
- `exchange` - Название биржи (binance, gate, bitget, bybit)
- `market` - Тип рынка (spot, linear)
- `symbol` - Торговая пара (например, BTCUSDT)
- `user` - Имя пользователя
- `ts_from` - Timestamp начала (миллисекунды)
- `ts_to` - Timestamp конца (миллисекунды)
- `delta_min` - Минимальная дельта (%)
- `delta_max` - Максимальная дельта (%)
- `volume_min` - Минимальный объём (USDT)
- `volume_max` - Максимальный объём (USDT)
- `limit` - Лимит записей (по умолчанию: 50)
- `offset` - Смещение (по умолчанию: 0)

#### GET `/api/spikes/stats`
Получить общую статистику стрел.

**Query параметры:**
- `period` - Период в днях (7, 14, 30, 60, 90, 180, 365)
- `exchange` - Фильтр по бирже
- `market` - Фильтр по рынку

#### GET `/api/users/{user}/spikes/stats`
Получить статистику стрел для пользователя.

**Query параметры:**
- `period` - Период в днях
- `exchange` - Фильтр по бирже
- `market` - Фильтр по рынку

#### GET `/api/users/{user}/spikes/by-symbol/{symbol}`
Получить стрелы пользователя по символу.

**Query параметры:**
- `period` - Период в днях

#### DELETE `/api/users/{user}/spikes`
Удалить все стрелы пользователя.

### Ошибки

#### GET `/api/errors`
Получить логи ошибок.

**Query параметры:**
- `exchange` - Фильтр по бирже
- `error_type` - Фильтр по типу ошибки
- `limit` - Лимит записей

#### DELETE `/api/errors/{error_id}`
Удалить конкретную ошибку (только для пользователя "Влад").

#### DELETE `/api/errors`
Удалить все ошибки (только для пользователя "Влад").

### Система

#### GET `/api/health`
Проверка здоровья системы.

**Возвращает:**
- `status` - Статус системы
- `uptime_seconds` - Время работы в секундах
- `process` - Метрики процесса (память, CPU)
- `system` - Системные метрики

#### GET `/api/status`
Статус работы бирж.

#### GET `/api/metrics`
Метрики системы.

#### GET `/api/exchanges/stats`
Статистика работы бирж.

---

## 💾 База данных

### Структура

База данных SQLite находится в `BD/detected_alerts.db`.

#### Таблица `users`
Хранит пользователей и их настройки.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | PRIMARY KEY |
| user | TEXT | Уникальное имя пользователя |
| password_hash | TEXT | Хеш пароля |
| tg_token | TEXT | Telegram bot token |
| chat_id | TEXT | Telegram Chat ID |
| options_json | TEXT | JSON с настройками (thresholds, exchanges, exchangeSettings, messageTemplates, etc.) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

#### Таблица `alerts`
Хранит историю обнаруженных стрел.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | PRIMARY KEY |
| ts | INTEGER | Timestamp в миллисекундах |
| exchange | TEXT | Название биржи |
| market | TEXT | Тип рынка (spot/linear) |
| symbol | TEXT | Торговая пара |
| delta | REAL | Изменение цены (%) |
| wick_pct | REAL | Процент тени свечи (%) |
| volume_usdt | REAL | Объём в USDT |
| meta | TEXT | Дополнительная информация (JSON) |
| created_at | TIMESTAMP | Дата создания |

**Индексы:**
- `idx_alerts_ts_exchange_market` - для фильтрации по времени, бирже и рынку
- `idx_alerts_user_id` - для фильтрации по пользователю
- `idx_alerts_symbol` - для фильтрации по символу

#### Таблица `errors`
Хранит логи ошибок для диагностики.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | PRIMARY KEY |
| timestamp | TIMESTAMP | Время возникновения |
| exchange | TEXT | Название биржи |
| error_type | TEXT | Тип ошибки (reconnect, websocket_error, etc.) |
| error_message | TEXT | Сообщение об ошибке |
| connection_id | TEXT | ID соединения |
| market | TEXT | Тип рынка |
| symbol | TEXT | Торговая пара |
| stack_trace | TEXT | Стек трейс (опционально) |

**Индексы:**
- `idx_errors_timestamp_exchange` - для фильтрации по времени и бирже

#### Другие таблицы
- `registration_whitelist` - Белый список логинов для регистрации
- `exchange_blacklists` - Чёрные списки символов по биржам
- `symbol_aliases` - Алиасы символов
- `exchange_statistics` - Статистика работы бирж

Подробная документация: [BD/README.md](BD/README.md)

---

## ⚙️ Конфигурация

### Файл `config.py`

```python
from config import config

# Уровень логирования
config.log_level = "INFO"  # DEBUG, INFO, WARNING, ERROR

# Максимальное количество свечей в памяти на символ
config.memory_max_candles_per_symbol = 1000

# Включение/выключение бирж и рынков
config.exchanges.gate_spot = True
config.exchanges.gate_linear = True
config.exchanges.binance_spot = True
config.exchanges.binance_linear = True
config.exchanges.bitget_spot = True
config.exchanges.bitget_linear = True
config.exchanges.bybit_spot = True
config.exchanges.bybit_linear = True
config.exchanges.hyperliquid_spot = True
config.exchanges.hyperliquid_linear = True
```

### Настройки пользователя (options_json)

Каждый пользователь может настроить свои фильтры через `options_json`:

```json
{
  "thresholds": {
    "delta_pct": 1.0,
    "volume_usdt": 10000.0,
    "shadow_pct": 50.0
  },
  "exchanges": {
    "gate": true,
    "binance": true,
    "bitget": true,
    "bybit": true
  },
  "exchangeSettings": {
    "gate": {
      "spot": {
        "delta_pct": 1.5,
        "volume_usdt": 20000.0,
        "shadow_pct": 40.0
      },
      "linear": {
        "delta_pct": 2.0,
        "volume_usdt": 15000.0
      }
    },
    "binance": {
      "spot": {
        "delta_pct": 1.0,
        "volume_usdt": 50000.0
      }
    }
  },
  "messageTemplates": [
    {
      "enabled": true,
      "conditions": {
        "delta_pct_from": 2.0,
        "delta_pct_to": "∞",
        "volume_usdt_min": 50000.0
      },
      "template": "🔥 Большая стрела: {symbol} {delta_pct}%",
      "separateChat": false,
      "separateChatId": ""
    }
  ],
  "seriesDetects": {
    "enabled": true,
    "timeWindowSeconds": 300,
    "maxCount": 3
  }
}
```

---

## 📱 Уведомления Telegram

### Настройка бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Получите Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Укажите токен и Chat ID в настройках пользователя

### Формат сообщений

По умолчанию сообщения отправляются в формате:
```
📈 BTCUSDT Binance Spot
Delta: +2.5%
Volume: 150,000 USDT
Wick: 45%
```

### Условные форматы

Пользователь может настроить условные форматы сообщений с:
- Диапазонами дельты (от/до)
- Минимальным объёмом
- Кастомными шаблонами
- Отдельными Telegram чатами для каждого условия

### Кастомные emoji

Система поддерживает кастомные emoji из пака [Strelk167](https://t.me/addemoji/Strelk167):
- Зелёная стрела вверх для роста
- Красная стрела вниз для падения

---

## 🔍 Детекция стрел

### Алгоритм детекции

1. **Построение свечей**: Система строит 1-секундные свечи из потоков сделок
2. **Анализ свечей**: Для каждой завершённой свечи проверяются фильтры всех пользователей
3. **Проверка условий**: 
   - Дельта (изменение цены) >= порог
   - Объём >= минимальный объём
   - Тень свечи >= порог (опционально)
   - Биржа и рынок включены пользователем
4. **Сохранение**: Если все условия выполнены, стрела сохраняется в БД
5. **Уведомление**: Отправляется уведомление в Telegram (если настроено)

### Фильтры

- **Дельта (%)**: Изменение цены за свечу
- **Объём (USDT)**: Объём торговли за свечу
- **Тень (%)**: Процент тени свечи относительно тела
- **Биржа**: Какие биржи мониторить
- **Рынок**: Spot или Futures/Linear

### Логика работы фильтров и приоритет настроек

Система использует многоуровневую систему фильтров с четким приоритетом проверки настроек:

#### Приоритет настроек (от высшего к низшему)

1. **`pairSettings`** (индивидуальные настройки по парам) - наивысший приоритет
   - Формат ключа: `{exchange}_{market}_{quote_currency}`
   - Примеры: `binance_futures_USDT`, `gate_spot_USDT`, `hyperliquid_spot_USDC`
   - Применяется только для конкретной пары (например, все USDT пары на Binance Futures)

2. **`exchangeSettings`** (настройки по бирже и рынку)
   - Формат: `exchangeSettings["{exchange}"]["{market}"]`
   - Примеры: `exchangeSettings["binance"]["futures"]`, `exchangeSettings["gate"]["spot"]`
   - Применяется для всех пар на указанной бирже и рынке

3. **`thresholds`** (глобальные пороги) - низший приоритет
   - Глобальные настройки для всех бирж и рынков
   - Применяется, если нет настроек на уровнях выше

#### Важное правило: явное намерение пользователя

**Если у пользователя отсутствуют фильтры или они отключены для какой-то биржи/рынка/пары, значит пользователь не хочет получать сигналы оттуда.**

##### Правило для `pairSettings`:

Если у пользователя есть хотя бы одна настройка в `pairSettings` для конкретной биржи и рынка (например, `binance_futures_USDT`), но для текущей обрабатываемой пары нет индивидуальных настроек (например, `binance_futures_BTC` отсутствует), то:

- ✅ Детектирование **НЕ применяется** для этой пары
- ✅ Даже если есть `exchangeSettings` или глобальные `thresholds` - они **игнорируются**
- ✅ Это означает, что пользователь **явно не настроил** фильтры для этой пары и не хочет получать сигналы

**Пример:**
```
У пользователя есть:
- pairSettings: { "binance_futures_USDT": { delta: 1.5, volume: 10000, shadow: 50 } }
- exchangeSettings: { "binance": { "futures": { delta: 1.0, volume: 5000, shadow: 40 } } }

При обработке свечи BTCUSDT (Binance Futures):
- Проверяется pairSettings["binance_futures_BTC"] - не найдено
- Обнаружено, что есть другие pairSettings для binance_futures_*
- Результат: детектирование НЕ применяется (пользователь не настроил фильтры для BTC)
- exchangeSettings игнорируются

При обработке свечи ETHUSDT (Binance Futures):
- Проверяется pairSettings["binance_futures_USDT"] - найдено
- Применяются индивидуальные фильтры (delta: 1.5, volume: 10000, shadow: 50)
```

##### Правило для `exchangeSettings`:

Если у пользователя нет настроек в `pairSettings` для биржи и рынка, но есть настройки в `exchangeSettings`, то:

- ✅ Применяются настройки из `exchangeSettings` для всех пар на этой бирже и рынке
- ✅ Если для конкретной биржи/рынка в `exchangeSettings` настройки отсутствуют или отключены (`enabled: false`), то детектирование **НЕ применяется**

##### Правило для `thresholds`:

Если у пользователя нет настроек в `pairSettings` и `exchangeSettings`, то:

- ✅ Применяются глобальные `thresholds` для всех бирж и рынков
- ✅ Если `thresholds` отсутствуют или не заполнены, то детектирование **НЕ применяется**

#### Включение/отключение бирж

Помимо фильтров порогов, пользователь может включать/отключать целые биржи через настройку `exchanges`:

```json
{
  "exchanges": {
    "binance": true,
    "gate": false,  // Gate.io полностью отключена
    "bitget": true,
    "bybit": true,
    "hyperliquid": true
  }
}
```

Если биржа отключена (`false`), то детектирование для неё **не применяется** независимо от других настроек.

### Серийные детекты

Система может фильтровать повторные стрелы по одному символу в заданном временном окне:
- **Временное окно**: Например, 300 секунд (5 минут)
- **Максимальное количество**: Например, 3 стрелы за окно

---

## 📊 Логирование и мониторинг

### Система логирования

Логи сохраняются в:
- **Консоль**: Вывод в реальном времени
- **Файлы**: `logs/app.log` с ротацией (10 MB, 5 файлов)
- **База данных**: Таблица `errors` для критических ошибок

### Уровни логирования

- **DEBUG**: Детальная информация для отладки
- **INFO**: Общая информация о работе
- **WARNING**: Предупреждения
- **ERROR**: Ошибки

Настройка в `config.py`:
```python
config.log_level = "INFO"
```

### Мониторинг здоровья

Система автоматически отслеживает:
- Использование памяти процесса
- Использование CPU процесса
- Использование памяти системы
- Количество потоков и файловых дескрипторов
- Время работы (uptime)

Heartbeat выводится каждые 60 секунд (если включен).

### Статистика бирж

Каждые 30 секунд выводится статистика:
```
binance spot - 250 пар   собрано свечей - 15,4к  сколько WS используется - 2  реконнекты WS - 0
binance linear - 180 пар   собрано свечей - 10,8к  сколько WS используется - 2  реконнекты WS - 0
```

Статистика включает:
- Количество активных символов
- Количество собранных свечей
- Количество WebSocket соединений
- Количество реконнектов

---

## ⚡ Производительность

### Оптимизации

- **Индексы БД**: Все частые запросы используют индексы
- **Кэширование**: Кэш пользователей на 5 секунд для быстрого обновления настроек
- **Асинхронность**: Все операции I/O выполняются асинхронно
- **Параллельность**: Биржи подключаются параллельно
- **Профилирование**: Инструменты для профилирования CPU и памяти

### Рекомендации по серверу

**Минимальная конфигурация:**
- CPU: 2 ядра
- RAM: 4 GB
- Storage: 50 GB SSD
- Network: 100 Mbps

**Рекомендуемая конфигурация:**
- CPU: 4 ядра
- RAM: 8 GB
- Storage: 100 GB SSD
- Network: 1 Gbps

### Лимиты подключений

- **Binance**: 150 стримов на соединение
- **Gate.io**: 135 символов Spot, 100 Linear на соединение
- **Bitget**: 39 символов Spot, 100 Linear на соединение
- **Bybit**: 86 символов Spot, 100 Linear на соединение
- **Hyperliquid**: Зависит от биржи

---

## 🚢 Развёртывание

### Локальное развёртывание

1. Установите зависимости (см. [Установка](#установка-и-настройка))
2. Запустите `main.py` для сбора данных
3. Запустите `api_server.py` для API
4. Запустите `WEB` для веб-интерфейса

### Production развёртывание на Windows Vultr сервер

**Быстрый старт:**
Смотрите [QUICK_START.md](QUICK_START.md) для краткой инструкции по деплою.

**Подробная документация:**
Смотрите [DEPLOY_WINDOWS.md](DEPLOY_WINDOWS.md) для детальной инструкции по деплою на Windows Vultr сервер.

**Основные шаги:**
1. Подготовка GitHub репозитория (используйте `setup-git.bat` на Windows)
2. Установка ПО на сервере (Python, Node.js, Git)
3. Клонирование репозитория на сервер
4. Настройка DNS и домена
5. Установка служб Windows через NSSM
6. Настройка Nginx как reverse proxy
7. Настройка автоматической синхронизации через Task Scheduler

**Автоматическая синхронизация:**
- Скрипт `deploy.ps1` автоматически проверяет обновления каждые 5 минут
- При обнаружении изменений код обновляется и службы перезапускаются
- Редактируйте код на локальном ПК → `git push` → автоматический деплой на сервер

### Production развёртывание (старая документация)

#### Systemd сервисы

Создайте сервис для основного модуля:

```ini
[Unit]
Description=Crypto Spikes Detector
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/onlyWS
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Создайте сервис для API:

```ini
[Unit]
Description=Crypto Spikes API
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/onlyWS
ExecStart=/usr/bin/python3 api_server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### Nginx reverse proxy

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api {
        proxy_pass http://localhost:8001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### SSL сертификат (Let's Encrypt)

```bash
sudo certbot --nginx -d your-domain.com
```

---

## ❓ FAQ

### Как добавить новую биржу?

1. Создайте папку в `exchanges/` с именем биржи
2. Реализуйте модули:
   - `ws_handler.py` - WebSocket клиент
   - `symbol_fetcher.py` - Получение списка символов
3. Добавьте биржу в `main.py` (ADAPTERS)
4. Добавьте переключатели в `config.py`

### Как настроить фильтры для конкретной биржи?

В настройках пользователя используйте `exchangeSettings`:

```json
{
  "exchangeSettings": {
    "binance": {
      "spot": {
        "delta_pct": 1.5,
        "volume_usdt": 50000.0
      }
    }
  }
}
```

### Как включить условные форматы сообщений?

В `options_json` добавьте `messageTemplates`:

```json
{
  "messageTemplates": [
    {
      "enabled": true,
      "conditions": {
        "delta_pct_from": 2.0,
        "delta_pct_to": "∞",
        "volume_usdt_min": 50000.0
      },
      "template": "🔥 {symbol} {delta_pct}%",
      "separateChat": false
    }
  ]
}
```

### Как получить отдельный Telegram чат для условия?

В настройках условного формата включите "Использовать отдельный чат" и укажите Chat ID.

### Как фильтровать серийные детекты?

В `options_json` добавьте `seriesDetects`:

```json
{
  "seriesDetects": {
    "enabled": true,
    "timeWindowSeconds": 300,
    "maxCount": 3
  }
}
```

### Как очистить старые стрелы?

Через веб-интерфейс в разделе статистики или через API:
```
DELETE /api/users/{user}/spikes
```

### Как посмотреть логи ошибок?

Через веб-интерфейс в админ панели (`/admin`) или через API:
```
GET /api/errors
```

### Сколько пользователей поддерживается?

Система поддерживает неограниченное количество пользователей. Каждый пользователь может иметь свои настройки фильтров.

### Можно ли запустить только определённые биржи?

Да, настройте в `config.py` какие биржи и рынки включить.

### Требуются ли API ключи бирж?

Нет, система работает только с публичными данными (WebSocket потоки). API ключи не требуются.

---

## 📝 Лицензия

Проект для личного использования.

---

## 🔗 Полезные ссылки

- [Быстрый старт: Деплой на Windows Vultr](QUICK_START.md)
- [Подробная инструкция по деплою](DEPLOY_WINDOWS.md)
- [Документация веб-интерфейса](WEB/README.md)
- [Документация базы данных](BD/README.md)
- [Документация Binance](exchanges/binance/README_binance.md)
- [Документация Gate.io](exchanges/gate/README_GATE.md)
- [Документация Bitget](exchanges/bitget/README_bitget.md)
- [Документация Bybit](exchanges/bybit/README_bybit.md)

---

**Последнее обновление**: 2024
