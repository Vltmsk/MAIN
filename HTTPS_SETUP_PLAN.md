# План настройки HTTPS для monitoringcrypto.ru на VPS Vultr

## 📋 Общая информация

**Цель:** Настроить HTTPS (защищённое соединение) для сайта monitoringcrypto.ru  
**Платформа:** Windows VPS Vultr  
**Домен:** Куплен на reg.ru  
**Сертификат:** Let's Encrypt (бесплатный)

---

## 📑 Оглавление (быстрая навигация)

### Основные шаги:
- [Шаг 1: Проверка DNS настроек](#-шаг-1-проверка-dns-настроек-на-regru) ✅
- [Шаг 2: Выбор веб-сервера](#-шаг-2-выбор-веб-сервера-для-windows) ✅
- [Шаг 3: Установка Nginx](#-шаг-3-установка-nginx-на-windows) ✅
- [Шаг 4: Получение SSL сертификата](#-шаг-4-получение-ssl-сертификата-lets-encrypt) ✅
- [**Шаг 5: Настройка Nginx с HTTPS**](#-шаг-5-настройка-nginx-как-reverse-proxy-с-https) ⬅️ **ТЕКУЩИЙ**
- [Шаг 6: Настройка файрвола](#-шаг-6-настройка-файрвола-windows)
- [Шаг 7: Обновление конфигурации](#-шаг-7-обновление-конфигурации-приложения)
- [Шаг 8: Проверка работы HTTPS](#-шаг-8-проверка-работы-https)
- [Шаг 9: Автоматизация](#-шаг-9-автоматизация-и-мониторинг)

### Полезные разделы:
- [Что делать дальше](#-что-делать-дальше-следующие-шаги)
- [Решение проблем](#-решение-проблем)
- [Чек-лист выполнения](#-чек-лист-выполнения)

---

## 📊 Прогресс выполнения

### ✅ Выполнено:

- [x] **Шаг 1:** DNS записи настроены (предположительно, так как сертификат получен)
- [x] **Шаг 2:** Выбран Nginx как веб-сервер
- [x] **Шаг 3:** Nginx установлен и работает (порт 80 занят Nginx)
- [x] **Шаг 4:** SSL сертификат получен от Let's Encrypt через Win-ACME
  - [x] Сертификат создан для `monitoringcrypto.ru` и `www.monitoringcrypto.ru`
  - [x] PEM файлы экспортированы в `C:\nginx\ssl\`
  - [x] **Файлы созданы:** `monitoringcrypto.ru-crt.pem`, `monitoringcrypto.ru-key.pem`, `monitoringcrypto.ru-chain.pem`
  - [x] Настроена автоматическая задача обновления в Task Scheduler
  - [x] Следующее обновление: после 2026.1.17

### 🔄 В процессе:

- [ ] **Шаг 5:** Настройка Nginx как Reverse Proxy с HTTPS
- [ ] **Шаг 6:** Настройка файрвола Windows (порты 80 и 443)
- [ ] **Шаг 7:** Обновление конфигурации приложения
- [ ] **Шаг 8:** Проверка работы HTTPS

### ⏳ Ожидает выполнения:

- [ ] **Шаг 9:** Автоматизация и мониторинг

---

## 🎯 Что делать дальше (следующие шаги)

### Сейчас нужно выполнить:

1. ✅ **Файлы сертификата созданы!** (выполнено)
   - `monitoringcrypto.ru-crt.pem` — сертификат
   - `monitoringcrypto.ru-key.pem` — приватный ключ
   - `monitoringcrypto.ru-chain.pem` — полная цепочка

2. **Настроить Nginx для HTTPS (Шаг 5):** ⬅️ **ТЕКУЩИЙ ШАГ**
   
   **📋 Быстрая шпаргалка:**
   - Файл конфигурации: `C:\nginx\conf\nginx.conf`
   - Файлы сертификата: `C:\nginx\ssl\monitoringcrypto.ru-chain.pem` и `C:\nginx\ssl\monitoringcrypto.ru-key.pem`
   - Полная инструкция: [см. Шаг 5 ниже](#-шаг-5-настройка-nginx-как-reverse-proxy-с-https)
   
   **⚡ Команды для проверки и перезапуска:**
   ```powershell
   # Проверка конфигурации
   cd C:\nginx
   .\nginx.exe -t
   
   # Перезапуск
   .\nginx.exe -s stop
   .\nginx.exe
   ```

4. **Проверить конфигурацию Nginx:**
   ```powershell
   cd C:\nginx
   .\nginx.exe -t
   ```

5. **Перезапустить Nginx:**
   ```powershell
   # Остановить
   Stop-Process -Name nginx -Force
   # Запустить
   cd C:\nginx
   .\nginx.exe
   ```

6. **Открыть порты 80 и 443 в файрволе (Шаг 6)**

7. **Проверить работу HTTPS (Шаг 8)**

---

## ✅ Шаг 1: Проверка DNS настроек на reg.ru

### 1.1. Вход в панель управления reg.ru

1. Зайдите на сайт [reg.ru](https://www.reg.ru)
2. Войдите в личный кабинет
3. Перейдите в раздел "Домены"
4. Найдите домен `monitoringcrypto.ru`
5. Откройте настройки домена

### 1.2. Настройка DNS записей

**Важно:** Нужно узнать IP-адрес вашего VPS Vultr сервера.

**Как узнать IP VPS:**
- Зайдите в панель управления Vultr
- Найдите ваш сервер
- Скопируйте IP-адрес (например, `123.45.67.89`)

**Настройте следующие DNS записи:**

1. **A-запись для основного домена:**
   - **Имя:** `@` или оставьте пустым
   - **Тип:** `A`
   - **Значение:** IP-адрес вашего VPS (например, `123.45.67.89`)
   - **TTL:** `3600` (или по умолчанию)

2. **A-запись для www:**
   - **Имя:** `www`
   - **Тип:** `A`
   - **Значение:** IP-адрес вашего VPS (тот же, что выше)
   - **TTL:** `3600` (или по умолчанию)

### 1.3. Проверка DNS

После настройки DNS подождите 5-30 минут для распространения изменений.

**Проверка через командную строку Windows:**
```powershell
nslookup monitoringcrypto.ru
nslookup www.monitoringcrypto.ru
```

Оба должны показывать IP-адрес вашего VPS.

**Онлайн проверка:**
- Используйте [dnschecker.org](https://dnschecker.org)
- Введите `monitoringcrypto.ru` и проверьте, что везде показывается ваш IP

---

## ✅ Шаг 2: Выбор веб-сервера для Windows

На Windows VPS есть два основных варианта:

### Вариант A: IIS (встроен в Windows Server)
- ✅ Уже установлен на Windows Server
- ✅ Интеграция с Windows
- ⚠️ Сложнее настройка SSL через Let's Encrypt

### Вариант B: Nginx (рекомендуется)
- ✅ Проще настройка
- ✅ Лучше для reverse proxy
- ✅ Легче получить Let's Encrypt сертификат
- ⚠️ Нужно установить

**Рекомендация:** Используйте **Nginx** (Вариант B), так как он проще в настройке для вашего случая.

---

## ✅ Шаг 3: Установка Nginx на Windows

### 3.1. Скачивание Nginx

1. Откройте браузер на VPS
2. Перейдите на [nginx.org/en/download.html](https://nginx.org/en/download.html)
3. Скачайте **Windows версию** (например, `nginx/Windows-1.25.3`)
4. Распакуйте архив в папку `C:\nginx`

### 3.2. Установка Nginx как службы Windows

**Способ 1: Через NSSM (рекомендуется)**

1. Скачайте NSSM: [nssm.cc/download](https://nssm.cc/download)
2. Распакуйте NSSM в `C:\nssm`
3. Откройте PowerShell **от имени администратора**
4. Выполните:

```powershell
cd C:\nssm\win64
.\nssm install Nginx "C:\nginx\nginx.exe"
.\nssm set Nginx AppDirectory "C:\nginx"
.\nssm start Nginx
```

**Способ 2: Через Task Scheduler (альтернатива)**

1. Откройте "Планировщик заданий" (Task Scheduler)
2. Создайте новое задание
3. Настройте запуск `C:\nginx\nginx.exe` при загрузке системы

### 3.3. Проверка работы Nginx

1. Откройте браузер на VPS
2. Перейдите на `http://localhost` или `http://127.0.0.1`
3. Должна открыться страница "Welcome to nginx!"

**Если не работает:**
- Проверьте, что порт 80 не занят другим приложением
- Проверьте файрвол Windows (должен разрешать входящие соединения на порт 80)

---

## ✅ Шаг 4: Получение SSL сертификата Let's Encrypt

### 4.1. Установка Certbot для Windows

Let's Encrypt работает через специальные инструменты. Для Windows VPS **рекомендуется использовать Win-ACME** (самый простой и надёжный способ).

**⚠️ Важно:** WSL может не работать на Windows Server или некоторых версиях Windows. Если команда `wsl --install` выдаёт ошибку, используйте Win-ACME.

**Вариант 1: Через Win-ACME (РЕКОМЕНДУЕТСЯ для Windows VPS)**

1. Скачайте Win-ACME: [win-acme.com](https://www.win-acme.com/)
   - Перейдите на страницу загрузки
   - Скачайте последнюю версию (например, `win-acme.v2.2.x.x.zip`)
2. Распакуйте архив в папку `C:\win-acme`
3. Запустите `wacs.exe` от имени администратора

**Вариант 2: Через WSL (альтернатива, может не работать на Windows Server)**

⚠️ **Внимание:** Этот способ может не работать на Windows Server или старых версиях Windows.

1. Попробуйте установить WSL:
```powershell
wsl --install
```
2. Если команда не найдена, используйте Win-ACME (Вариант 1)
3. Если WSL установился, перезагрузите компьютер
4. В WSL установите Certbot:
```bash
sudo apt update
sudo apt install certbot
```

**Вариант 3: Ручное получение через Certbot на другом сервере**

Если у вас есть доступ к Linux серверу, можно получить сертификат там и перенести файлы на Windows.

### 4.2. Получение сертификата через Win-ACME (РЕКОМЕНДУЕМЫЙ способ)

#### Шаг 1: Запуск Win-ACME

1. Откройте PowerShell **от имени администратора**
2. Перейдите в папку Win-ACME:
```powershell
cd C:\win-acme
.\wacs.exe
```

#### Шаг 2: Интерактивная настройка

Win-ACME запустится в интерактивном режиме. Следуйте инструкциям:

1. **Выберите действие из главного меню:**
   - Выберите `N` (Create certificate with default settings) - самый простой вариант
   - Или `M` (Create certificate with full options) - если нужны дополнительные настройки

2. **Определение доменов (если выбрали N):**
   
   Программа спросит: "How shall we determine the domain(s) to include in the certificate?"
   
   **⚠️ Важно:** Если вы видите сообщение "Source plugin IIS not available: No supported version of IIS detected" - это нормально! Вы используете Nginx, а не IIS.
   
   **Выберите вариант:**
   - `2` - **Manual input** (ручной ввод) ← **ВЫБЕРИТЕ ЭТОТ ВАРИАНТ**
   
   **Почему вариант 2:**
   - Вариант 1 (Read bindings from IIS) не подходит, так как IIS не установлен
   - Вариант 2 позволяет вручную ввести домены
   - Вариант 3 (CSR) нужен только для специальных случаев

3. **Ввод доменов:**
   
   После выбора "Manual input" программа попросит ввести домены.
   
   **Введите домены через запятую:**
   ```
   monitoringcrypto.ru, www.monitoringcrypto.ru
   ```
   
   Или только основной домен (если www не нужен):
   ```
   monitoringcrypto.ru
   ```
   
   Нажмите Enter после ввода.

4. **Выбор способа проверки домена (Validation):**
   
   Программа предложит способы проверки:
   - `[Http]` - проверка через HTTP (рекомендуется) ← **ВЫБЕРИТЕ ЭТОТ**
   - `[Dns]` - проверка через DNS (сложнее, требует настройки DNS записей)
   
   **Выберите `[Http]`** - это самый простой способ. Win-ACME автоматически создаст временный файл на вашем веб-сервере для проверки.

5. **Выбор плагина для хранения (Storage):**
   
   Программа спросит, где хранить сертификат:
   - Обычно выбирается вариант по умолчанию (нажмите Enter)
   - Или выберите вариант, который сохраняет в файловую систему

6. **Выбор плагина для установки (Installation):**
   
   Программа спросит: "Which installation step should run first?"
   
   **⚠️ Важно для Nginx:**
   - Вариант 1: "Create or update bindings in IIS" - НЕ подходит (IIS не используется)
   - Вариант 2: "Start external script or program" - можно использовать, но не обязательно
   - **Вариант 3: "No (additional) installation steps"** ← **ВЫБЕРИТЕ ЭТОТ ВАРИАНТ**
   
   **Почему вариант 3:**
   - Win-ACME сохранит сертификат в файлы
   - Мы настроим Nginx вручную в следующем шаге (Шаг 5)
   - Это самый простой и понятный способ для Nginx
   
   **Выберите `3`** и нажмите Enter

7. **Условия использования Let's Encrypt:**
   
   Программа покажет путь к файлу с условиями использования:
   `Terms of service: C:\ProgramData\win-acme\...\LE-SA-v1.6-August-18-2025.pdf`
   
   И спросит: `Open in default application? (y/n*)`
   
   **Можно выбрать:**
   - `n` (нет) - не открывать файл (рекомендуется, быстрее)
   - `y` (да) - открыть PDF с условиями (если хотите прочитать)
   
   **Выберите `n`** и нажмите Enter - это не повлияет на получение сертификата.

8. **Согласие с условиями использования:**
   
   Программа спросит: `Do you agree with the terms? (y/n)`
   
   **⚠️ Важно:** Это обязательный шаг для получения сертификата от Let's Encrypt.
   
   **Выберите `y`** (да) и нажмите Enter, чтобы согласиться с условиями использования Let's Encrypt.

9. **Email для уведомлений:**
   
   Программа спросит: `Enter email(s) for notifications about problems and abuse (comma-separated):`
   
   **Это необязательное поле, но рекомендуется:**
   - Let's Encrypt будет отправлять уведомления о проблемах с сертификатом
   - Можно указать один или несколько email через запятую
   - Можно оставить пустым (просто нажать Enter)
   
   **Рекомендация:** Укажите ваш email, например:
   ```
   ваш-email@example.com
   ```
   
   Или несколько email через запятую:
   ```
   email1@example.com, email2@example.com
   ```
   
   Если не хотите указывать email, просто нажмите Enter (поле останется пустым).

10. **Подтверждение настроек:**
   
   Win-ACME покажет все выбранные настройки:
   - Домены для сертификата
   - Способ проверки (HTTP)
   - Способ установки (None)
   
   Проверьте, что домены указаны правильно:
   - `monitoringcrypto.ru`
   - `www.monitoringcrypto.ru` (если вы его добавили)
   
   **Нажмите `Y`** для подтверждения и начала получения сертификата

#### Шаг 3: Подготовка перед получением сертификата

**⚠️ ВАЖНО: Проблема с портом 80**

Если вы видите ошибку:
```
Unable to activate listener, this may be because of insufficient rights or a non-Microsoft webserver using port 80
The process cannot access the file because it is being used by another process
```

**Причина:** Порт 80 уже занят (скорее всего Nginx или другой веб-сервер).

**Решение: Временно остановить Nginx**

1. **Остановите Nginx:**
   ```powershell
   # Если Nginx установлен как служба:
   Stop-Service Nginx
   
   # Или если запущен напрямую:
   cd C:\nginx
   .\nginx.exe -s stop
   ```

2. **Проверьте, что порт 80 свободен:**
   ```powershell
   netstat -ano | findstr ":80"
   ```
   Должно быть пусто (или только `LISTENING` от других процессов, которые можно остановить)

3. **Теперь запустите Win-ACME снова:**
   ```powershell
   cd C:\win-acme
   .\wacs.exe
   ```
   
   Выберите `R` (Run renewals) или создайте сертификат заново (`N`)

4. **После успешного получения сертификата запустите Nginx обратно:**
   ```powershell
   # Если Nginx установлен как служба:
   Start-Service Nginx
   
   # Или если запускается напрямую:
   cd C:\nginx
   .\nginx.exe
   ```

**Альтернативное решение: Использовать DNS-проверку**

Если не хотите останавливать Nginx, можно использовать DNS-проверку вместо HTTP:
- При выборе способа проверки выберите `[Dns]` вместо `[Http]`
- Но это сложнее - нужно будет вручную добавлять TXT записи в DNS

#### Шаг 4: Автоматическая настройка

После того как порт 80 свободен, Win-ACME автоматически:
- Создаст временный файл для проверки домена
- Получит сертификат от Let's Encrypt
- Сохранит сертификат в папку
- Создаст задачу в Task Scheduler для автоматического обновления

#### Шаг 4: Экспорт сертификата в файлы для Nginx

**✅ ВЫПОЛНЕНО:** Сертификат успешно экспортирован в PEM файлы!

**Результат:**
- ✅ Настроено сохранение PEM файлов в `C:\nginx\ssl\`
- ✅ Папка `C:\nginx\ssl\` создана
- ✅ **Файлы сертификата созданы!**
- ✅ Настроена автоматическая задача обновления в Task Scheduler
- ✅ Следующее обновление: после 2026.1.17

**Созданные файлы в `C:\nginx\ssl\`:**
- ✅ `monitoringcrypto.ru-crt.pem` — сертификат
- ✅ `monitoringcrypto.ru-key.pem` — приватный ключ (без пароля)
- ✅ `monitoringcrypto.ru-chain.pem` — полная цепочка сертификатов (сертификат + цепочка)
- ✅ `monitoringcrypto.ru-chain-only.pem` — только цепочка сертификатов

**Для Nginx используйте:**
- **Сертификат:** `C:\nginx\ssl\monitoringcrypto.ru-chain.pem` (полная цепочка) или `monitoringcrypto.ru-crt.pem` (только сертификат)
- **Приватный ключ:** `C:\nginx\ssl\monitoringcrypto.ru-key.pem`

**Проверка файлов:**
```powershell
Get-ChildItem C:\nginx\ssl\
```

---

**⚠️ Информация (для справки):** Если бы Win-ACME установил сертификат только в Windows Certificate Store (а не в файлы), нужно было бы экспортировать его вручную.

**Способ 1: Через Win-ACME (рекомендуется)**

1. Запустите Win-ACME снова:
   ```powershell
   cd C:\win-acme
   .\wacs.exe
   ```

2. Выберите `A` (Manage renewals)

3. Выберите ваш сертификат (должен быть 1 total)

4. В меню управления сертификатом найдите опцию экспорта или сохранения в файлы

5. Выберите формат экспорта (PEM или PFX)

**Способ 2: Экспорт через PowerShell (если способ 1 не работает)**

1. Найдите сертификат в хранилище:
   ```powershell
   $cert = Get-ChildItem -Path Cert:\LocalMachine\My | Where-Object {$_.Subject -like "*monitoringcrypto.ru*"} | Select-Object -First 1
   ```

2. Если сертификат не найден в LocalMachine, проверьте CurrentUser:
   ```powershell
   $cert = Get-ChildItem -Path Cert:\CurrentUser\My | Where-Object {$_.Subject -like "*monitoringcrypto.ru*"} | Select-Object -First 1
   ```

3. Экспортируйте сертификат и приватный ключ:
   ```powershell
   # Создайте папку для сертификатов
   New-Item -ItemType Directory -Path "C:\nginx\ssl" -Force
   
   # Экспортируйте сертификат в PEM формате
   $certBytes = $cert.Export([System.Security.Cryptography.X509Certificates.X509ContentType]::Cert)
   [System.IO.File]::WriteAllBytes("C:\nginx\ssl\certificate.crt", $certBytes)
   
   # Экспортируйте приватный ключ (требует PFX с паролем)
   $password = ConvertTo-SecureString -String "temp_password" -Force -AsPlainText
   $pfxBytes = $cert.Export([System.Security.Cryptography.X509Certificates.X509ContentType]::Pfx, $password)
   [System.IO.File]::WriteAllBytes("C:\nginx\ssl\certificate.pfx", $pfxBytes)
   ```

**⚠️ Проблема:** Windows Certificate Store не позволяет легко экспортировать приватный ключ в PEM формате. Лучше использовать Win-ACME для экспорта.

**Способ 3: Пересоздать сертификат с сохранением в файлы**

1. Запустите Win-ACME:
   ```powershell
   cd C:\win-acme
   .\wacs.exe
   ```

2. Выберите `A` (Manage renewals)

3. Выберите ваш сертификат

4. Удалите текущий сертификат (или измените настройки хранения)

5. Создайте новый сертификат (`N`) и при выборе плагина хранения выберите вариант, который сохраняет в файловую систему (не в Certificate Store)

**Рекомендуемые пути для файлов сертификата:**
- **Certificate file:** `C:\nginx\ssl\certificate.crt`
- **Private key:** `C:\nginx\ssl\private.key`

Создайте папку для сертификатов:
```powershell
New-Item -ItemType Directory -Path "C:\nginx\ssl" -Force
```

#### Шаг 5: Проверка успешного получения

Если всё прошло успешно, вы увидите сообщение:
```
Certificate installed
```

Если была ошибка, проверьте:
- Правильно ли настроены DNS записи (домен должен указывать на IP вашего VPS)
- Открыт ли порт 80 в файрволе (нужен для проверки домена)
- Работает ли веб-сервер на порту 80

### 4.3. Автоматическое обновление сертификата

Win-ACME автоматически создаёт задачу в Task Scheduler для обновления сертификата каждые 60 дней (Let's Encrypt сертификаты действуют 90 дней).

---

## ✅ Шаг 5: Настройка Nginx как Reverse Proxy с HTTPS

> **📍 Быстрая навигация по шагу:**
> - [5.1. Создание конфигурации](#51-создание-конфигурации-nginx)
> - [5.2. Проверка конфигурации](#52-проверка-конфигурации-nginx)
> - [5.3. Перезапуск Nginx](#53-перезапуск-nginx)

**🎯 Краткая инструкция (3 шага):**
1. Откройте `C:\nginx\conf\nginx.conf` и добавьте конфигурацию ниже
2. Проверьте: `cd C:\nginx && .\nginx.exe -t`
3. Перезапустите: `.\nginx.exe -s stop && .\nginx.exe`

**💡 Совет: Если файл плохо отформатирован (всё в одну строку):**
- **Рекомендуется:** Использовать Notepad++ (бесплатный редактор) - он автоматически форматирует код
  - Скачать: https://notepad-plus-plus.org/downloads/
  - Откройте файл в Notepad++ → Вид → Перенос строки по словам (если нужно)
- **Или используйте поиск (Ctrl+F)** в обычном блокноте по инструкции ниже

---

### 5.1. Создание конфигурации Nginx

**📝 Что нужно сделать:**
1. Откройте файл `C:\nginx\conf\nginx.conf` в текстовом редакторе (Notepad++ или обычный Блокнот)

---

**🔍 ПРОСТОЙ СПОСОБ (если файл в одну строку или сложно найти нужное место):**

**Вариант А: Добавить в конец файла (самый простой способ)**
1. Откройте файл `C:\nginx\conf\nginx.conf`
2. Нажмите `Ctrl+End` (перейти в конец файла)
3. Найдите последнюю закрывающую скобку `}` (это конец блока `http`)
4. Поставьте курсор ПЕРЕД этой последней `}`
5. Нажмите `Enter` (новая строка)
6. Вставьте всю конфигурацию ниже (скопируйте весь блок кода)
7. Сохраните файл (`Ctrl+S`)

**Вариант Б: Использовать поиск для точного места**
1. Нажмите `Ctrl+F` в блокноте
2. Введите одно из этих слов и нажмите "Найти далее":
   - `location /api` - найти блок API
   - `http {` - найти начало HTTP блока
   - `}` - найти закрывающие скобки (нажимайте "Найти далее" несколько раз)
3. Вставьте конфигурацию в найденное место

---

**🔍 Если в блокноте всё в одну строку (плохо отформатировано):**

**Используйте поиск в блокноте (Ctrl+F) для нахождения нужных мест:**

**Вариант 1: Найти место для вставки конфигурации**
- Нажмите `Ctrl+F` (или Правка → Найти)
- Введите: `location /api` и нажмите "Найти далее"
- Или введите: `http {` и нажмите "Найти далее"
- **Вставьте новую конфигурацию ПЕРЕД блоком `location /api` или в конце блока `http {`**

**Вариант 2: Если есть старый блок server**
- Нажмите `Ctrl+F`
- Введите: `listen 80` и нажмите "Найти далее"
- Если найдётся, замените весь блок `server { ... }` на новую конфигурацию

**Вариант 3: Если ничего не найдено (простой файл)**
- Просто добавьте новую конфигурацию в конец файла (перед закрывающей скобкой `}` блока `http`)

2. **Добавьте настройку в начало блока `http {` (если её нет):**

**Важно:** Сначала добавьте эту строку в начало блока `http {` (чтобы избежать ошибки с хеш-таблицей):

```nginx
http {
    server_names_hash_bucket_size 64;  # ← Добавьте эту строку в начало блока http
    
    # ... остальная конфигурация ...
}
```

**Как добавить:**
- Нажмите `Ctrl+F` и найдите `http {`
- Поставьте курсор после открывающей скобки `{`
- Нажмите `Enter` и добавьте строку: `server_names_hash_bucket_size 64;`

3. **Добавьте следующую конфигурацию:**

```nginx
# Редирект HTTP на HTTPS
server {
    listen 80;
    server_name monitoringcrypto.ru www.monitoringcrypto.ru;
    
    # Редирект всех HTTP запросов на HTTPS
    return 301 https://$server_name$request_uri;
}

# HTTPS сервер
server {
    listen 443 ssl;
    http2 on;
    server_name monitoringcrypto.ru www.monitoringcrypto.ru;

    # SSL сертификаты
    # Используйте chain.pem для полной цепочки (рекомендуется) или crt.pem для только сертификата
    ssl_certificate C:/nginx/ssl/monitoringcrypto.ru-chain.pem;
    ssl_certificate_key C:/nginx/ssl/monitoringcrypto.ru-key.pem;

    # SSL настройки (безопасность)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Проксирование на Next.js (порт 3000)
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Проксирование API запросов на FastAPI (порт 8001)
    location /api {
        proxy_pass http://localhost:8001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**📋 Как использовать эту конфигурацию:**

1. **Выделите весь блок кода выше** (от `# Редирект HTTP на HTTPS` до последней `}`)
2. **Скопируйте** (`Ctrl+C`)
3. **Откройте файл** `C:\nginx\conf\nginx.conf` в блокноте
4. **Найдите место для вставки:**
   - Нажмите `Ctrl+End` (в конец файла)
   - Найдите последнюю `}` (закрывающая скобка блока `http`)
   - Поставьте курсор ПЕРЕД этой `}`
   - Нажмите `Enter` (новая строка)
5. **Вставьте** (`Ctrl+V`)
6. **Сохраните** (`Ctrl+S`)

**💡 Пример того, как должен выглядеть файл после вставки:**

```
http {
    # ... другие настройки ...
    
    location /api {
        proxy_pass http://api;
        # ... другие настройки ...
    }
    
    # ← ВСТАВЬТЕ НОВУЮ КОНФИГУРАЦИЮ ЗДЕСЬ (перед этой скобкой)
    
}  ← Это последняя закрывающая скобка блока http
```

**✅ Важные моменты:** 
- ✅ Пути к сертификатам уже указаны правильно: `C:/nginx/ssl/monitoringcrypto.ru-chain.pem` и `C:/nginx/ssl/monitoringcrypto.ru-key.pem`
- Используйте прямые слеши `/` в путях Windows для Nginx (Nginx понимает оба формата)
- Можно использовать `monitoringcrypto.ru-chain.pem` (полная цепочка, рекомендуется) или `monitoringcrypto.ru-crt.pem` (только сертификат)
- **Проксирование:** `/` → Next.js (порт 3000), `/api` → FastAPI (порт 8001)

**💡 Где вставить конфигурацию (пошагово):**

**Шаг 1: Найдите блок `http {`**
- Нажмите `Ctrl+F` в блокноте
- Введите: `http {` (без кавычек)
- Нажмите "Найти далее"
- Запомните это место

**Шаг 2: Найдите конец блока `http` или блок `location /api`**
- Нажмите `Ctrl+F` снова
- Введите: `location /api` или просто `}` (закрывающая скобка)
- Нажмите "Найти далее" несколько раз, пока не найдёте место перед `location /api` или перед последней `}` блока `http`

**Шаг 3: Вставьте новую конфигурацию**
- Поставьте курсор ПЕРЕД блоком `location /api` (если он есть)
- Или поставьте курсор ПЕРЕД последней закрывающей скобкой `}` блока `http`
- Вставьте всю конфигурацию ниже (скопируйте весь блок кода)

**⚠️ Важно:**
- Если в файле уже есть блок `server { listen 80; }`, найдите его через поиск `listen 80` и замените на новую конфигурацию
- Если ничего не найдено, просто добавьте конфигурацию в конец файла (перед последней `}`)

---

### 5.2. Проверка конфигурации Nginx

**📝 Проверка перед перезапуском (обязательно!):**

1. Откройте PowerShell
2. Перейдите в папку Nginx:
```powershell
cd C:\nginx
.\nginx.exe -t
```

**✅ Успешный результат:**
Должно вывести: `nginx: configuration file ... test is successful`

---

**🔧 Если появились ошибки - решения:**

**❌ Ошибка 1: `[emerg] could not build server_names_hash, you should increase server_names_hash_bucket_size: 32`**

**Причина:** Nginx не может создать хеш-таблицу для имён серверов (слишком длинные имена доменов)

**Решение:**
1. Откройте файл `C:\nginx\conf\nginx.conf`
2. Найдите блок `http {` (нажмите `Ctrl+F` и введите `http {`)
3. Внутри блока `http {` (в самом начале, после открывающей скобки) добавьте строку:
```nginx
http {
    server_names_hash_bucket_size 64;  # ← Добавьте эту строку
    
    # ... остальная конфигурация ...
}
```
4. Сохраните файл и проверьте снова: `.\nginx.exe -t`

**❌ Ошибка 2: `[warn] the "listen ... http2" directive is deprecated`**

**Причина:** Устаревший синтаксис для HTTP/2

**Решение:**
1. Найдите строку с `listen 443 ssl http2;` (нажмите `Ctrl+F`)
2. Замените на:
```nginx
listen 443 ssl;
http2 on;  # ← Добавьте эту строку отдельно
```
Или просто уберите `http2` из `listen`:
```nginx
listen 443 ssl;  # ← Без http2 здесь
http2 on;        # ← Добавьте отдельной строкой
```

**❌ Ошибка 3: `[warn] conflicting server name "monitoringcrypto.ru" on 0.0.0.0:80, ignored`**

**Причина:** В файле есть несколько блоков `server` с одинаковым `server_name` на порту 80

**Решение:**
1. Нажмите `Ctrl+F` и найдите все вхождения `server_name monitoringcrypto.ru`
2. Убедитесь, что у вас только ОДИН блок `server { listen 80; ... }` с этими доменами
3. Если есть дубликаты - удалите старые блоки `server { listen 80; ... }`, оставьте только новый блок из конфигурации выше
4. Проверьте снова: `.\nginx.exe -t`

**💡 Как найти и удалить дубликаты:**
- Нажмите `Ctrl+F`
- Введите: `listen 80`
- Нажмите "Найти далее" несколько раз
- Если найдётся несколько блоков `server { listen 80; ... }`, удалите старые, оставьте только один (новый из конфигурации выше)

**📋 Полная исправленная конфигурация (если нужно переделать):**

Если нужно исправить всё сразу, используйте эту версию (с исправлениями):

```nginx
# Добавьте в начало блока http {
http {
    server_names_hash_bucket_size 64;  # ← Исправление ошибки 1
    
    # ... другие настройки ...
    
    # Редирект HTTP на HTTPS
    server {
        listen 80;
        server_name monitoringcrypto.ru www.monitoringcrypto.ru;
        
        # Редирект всех HTTP запросов на HTTPS
        return 301 https://$server_name$request_uri;
    }

    # HTTPS сервер
    server {
        listen 443 ssl;  # ← Исправление ошибки 2 (убрали http2 отсюда)
        http2 on;         # ← Исправление ошибки 2 (добавили отдельно)
        server_name monitoringcrypto.ru www.monitoringcrypto.ru;

        # SSL сертификаты
        ssl_certificate C:/nginx/ssl/monitoringcrypto.ru-chain.pem;
        ssl_certificate_key C:/nginx/ssl/monitoringcrypto.ru-key.pem;

        # SSL настройки (безопасность)
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;

        # Проксирование на Next.js (порт 3000)
        location / {
            proxy_pass http://localhost:3000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Проксирование API запросов на FastAPI (порт 8001)
        location /api {
            proxy_pass http://localhost:8001;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
```

### 5.3. Перезапуск Nginx

**📝 Команды для перезапуска:**

**Вариант 1: Если Nginx запущен как процесс (обычный способ)**
```powershell
cd C:\nginx
# Остановить
.\nginx.exe -s stop
# Запустить
.\nginx.exe
```

**Вариант 2: Если Nginx установлен как служба Windows**
```powershell
Restart-Service Nginx
```

**✅ Проверка, что Nginx запущен:**
```powershell
# Проверить процессы
Get-Process nginx -ErrorAction SilentlyContinue

# Проверить, что порты слушаются
netstat -an | findstr ":80"
netstat -an | findstr ":443"
```

**⚠️ Если возникли ошибки:**
- Проверьте логи: `C:\nginx\logs\error.log`
- Убедитесь, что порты 80 и 443 не заняты другими процессами
- Проверьте права доступа к файлам сертификата

---

## ✅ Шаг 6: Настройка файрвола Windows

> **📍 Быстрая навигация:**
> - [6.1. Открытие портов](#61-открытие-портов)
> - [6.2. Проверка через PowerShell](#62-проверка-через-powershell)

**🎯 Краткая инструкция:**
1. Откройте "Брандмауэр Защитника Windows" → "Дополнительные параметры"
2. Выберите "Inbound Rules" → "New Rule..."
3. Создайте правило для портов 80 и 443 (TCP, Allow)

---

### 6.1. Открытие портов

**📝 Пошаговая инструкция:**

**Шаг 1: Откройте "Брандмауэр Защитника Windows"**
- Нажмите `Win + R` (или Пуск → Выполнить)
- Введите: `wf.msc` и нажмите Enter
- Или: Пуск → Поиск → "Брандмауэр Защитника Windows" → "Дополнительные параметры"

**Шаг 2: Выберите "Правила для входящих подключений"**
- В левой панели найдите и нажмите на **"Inbound Rules"** (Правила для входящих подключений)
- В правой панели "Actions" нажмите **"New Rule..."** (Создать правило...)

**Шаг 3: Мастер создания правила - Тип правила**
- Выберите: **"Port"** (Для порта)
- Нажмите **"Next"** (Далее)

**Шаг 4: Протокол и порты**
- Выберите: **"TCP"**
- Выберите: **"Specific local ports"** (Определенные локальные порты)
- В поле введите: **`80,443`** (порты через запятую)
- Нажмите **"Next"** (Далее)

**Шаг 5: Действие**
- Выберите: **"Allow the connection"** (Разрешить подключение)
- Нажмите **"Next"** (Далее)

**Шаг 6: Профили**
- Отметьте все три профиля:
  - ✅ **Domain** (Доменный)
  - ✅ **Private** (Частный)
  - ✅ **Public** (Публичный)
- Нажмите **"Next"** (Далее)

**Шаг 7: Имя правила**
- В поле "Name" введите: **"HTTP и HTTPS для Nginx"**
- В поле "Description" (опционально) можно ввести: "Разрешить входящие подключения на порты 80 (HTTP) и 443 (HTTPS) для веб-сервера Nginx"
- Нажмите **"Finish"** (Готово)

**✅ Готово!** Правило создано. Теперь порты 80 и 443 открыты для входящих подключений.

**💡 Альтернативный способ через PowerShell (быстрее):**

Если хотите создать правило через командную строку:

```powershell
# Создать правило для порта 80 (HTTP)
New-NetFirewallRule -DisplayName "HTTP для Nginx" -Direction Inbound -Protocol TCP -LocalPort 80 -Action Allow

# Создать правило для порта 443 (HTTPS)
New-NetFirewallRule -DisplayName "HTTPS для Nginx" -Direction Inbound -Protocol TCP -LocalPort 443 -Action Allow
```

**⚠️ Важно:** Запустите PowerShell от имени администратора (правой кнопкой → "Запуск от имени администратора")

### 6.2. Проверка через PowerShell

```powershell
# Проверить открытые порты
netstat -an | findstr ":80"
netstat -an | findstr ":443"
```

---

## ✅ Шаг 7: Обновление конфигурации приложения

### 7.1. Обновление переменных окружения

Убедитесь, что в вашем приложении правильно настроен домен:

**Для FastAPI (api_server.py):**
- Уже настроено через переменную окружения `DOMAIN`
- Установите: `$env:DOMAIN = "monitoringcrypto.ru"`

**Для Next.js:**
- Проверьте, что приложение работает на `localhost:3000`
- Nginx будет проксировать запросы

### 7.2. Обновление CORS настроек

В `api_server.py` уже есть поддержка HTTPS домена через переменную `DOMAIN`. Убедитесь, что она установлена:

```powershell
$env:DOMAIN = "monitoringcrypto.ru"
```

---

## ✅ Шаг 8: Проверка работы HTTPS

### 8.1. Локальная проверка на VPS

1. Откройте браузер на VPS
2. Перейдите на `https://monitoringcrypto.ru`
3. Должен появиться зелёный замок 🔒
4. Сайт должен открываться без предупреждений

### 8.2. Проверка с внешнего компьютера

1. Откройте браузер на любом другом компьютере
2. Перейдите на `https://monitoringcrypto.ru`
3. Проверьте:
   - ✅ Зелёный замок в адресной строке
   - ✅ Нет предупреждений о безопасности
   - ✅ Сайт открывается корректно
   - ✅ Все страницы работают

### 8.3. Проверка редиректа HTTP → HTTPS

1. Откройте `http://monitoringcrypto.ru` (без S)
2. Должен автоматически перенаправить на `https://monitoringcrypto.ru`

### 8.4. Онлайн проверка SSL

Используйте онлайн инструменты:
- [SSL Labs SSL Test](https://www.ssllabs.com/ssltest/) - введите `monitoringcrypto.ru`
- [SSL Checker](https://www.sslshopper.com/ssl-checker.html)

---

## ✅ Шаг 9: Автоматизация и мониторинг

### 9.1. Проверка автоматического обновления сертификата

Win-ACME должен автоматически обновлять сертификат. Проверьте:

1. Откройте "Планировщик заданий" (Task Scheduler)
2. Найдите задачу от Win-ACME
3. Убедитесь, что она настроена на автоматический запуск

### 9.2. Мониторинг работы Nginx

Создайте простой скрипт для проверки:

```powershell
# check-nginx.ps1
$nginx = Get-Process nginx -ErrorAction SilentlyContinue
if ($nginx) {
    Write-Host "Nginx работает"
} else {
    Write-Host "Nginx не запущен!"
    Start-Service Nginx
}
```

### 9.3. Логирование

Проверяйте логи Nginx:
- **Access log:** `C:\nginx\logs\access.log`
- **Error log:** `C:\nginx\logs\error.log`

---

## 🔧 Решение проблем

### Проблема 0: "wsl: команда не найдена" или "CommandNotFoundException"

**Причина:** WSL не установлен или не поддерживается на вашей версии Windows  
**Решение:** 
- ✅ **Используйте Win-ACME вместо WSL** (это рекомендуемый способ для Windows VPS)
- Win-ACME работает на всех версиях Windows, включая Windows Server
- Не требует установки дополнительных компонентов
- Просто скачайте и запустите `wacs.exe`

**Как перейти на Win-ACME:**
1. Скачайте Win-ACME: [win-acme.com](https://www.win-acme.com/)
2. Распакуйте в `C:\win-acme`
3. Запустите `wacs.exe` от имени администратора
4. Следуйте инструкциям из раздела 4.2

### Проблема 1: "Сертификат не доверенный"

**Причина:** Браузер не доверяет Let's Encrypt сертификату  
**Решение:** 
- Убедитесь, что сертификат получен правильно
- Проверьте, что цепочка сертификатов полная
- В Win-ACME выберите опцию "Полная цепочка сертификатов"

### Проблема 2: "Порт 80/443 занят" или "Unable to activate listener" в Win-ACME

**Причина:** Порт 80 уже занят (обычно Nginx или другой веб-сервер), и Win-ACME не может использовать его для проверки домена.

**Ошибка в Win-ACME:**
```
Unable to activate listener, this may be because of insufficient rights or a non-Microsoft webserver using port 80
The process cannot access the file because it is being used by another process
```

**Решение:**

1. **Проверьте, что использует порт 80:**
```powershell
netstat -ano | findstr ":80"
```

Это покажет процессы, использующие порт 80. Обратите внимание на последний столбец (PID - идентификатор процесса).

2. **Проверьте, запущен ли Nginx:**
```powershell
# Проверить процессы Nginx
Get-Process nginx -ErrorAction SilentlyContinue

# Или проверить службу Nginx (если установлен как служба)
Get-Service Nginx -ErrorAction SilentlyContinue
```

3. **Остановите процесс, который занимает порт 80:**

**Если Nginx запущен как процесс:**
```powershell
# Найти PID процесса Nginx
Get-Process nginx | Select-Object Id

# Остановить процесс по PID (замените PID на реальный номер)
Stop-Process -Id <PID> -Force
```

**Если Nginx установлен как служба:**
```powershell
Stop-Service Nginx
```

**Если Nginx не запущен, но порт 80 занят другим процессом:**
```powershell
# Найти PID процесса, который использует порт 80
$port80 = netstat -ano | findstr ":80" | findstr "LISTENING"
# Посмотрите последний столбец - это PID

# Остановить процесс по PID (замените <PID> на реальный номер из вывода выше)
Stop-Process -Id <PID> -Force
```

**Если порт 80 занят IIS:**
```powershell
# Остановить IIS
Stop-Service W3SVC
```

4. **Проверьте, что порт 80 свободен:**
```powershell
netstat -ano | findstr ":80"
# Должно быть пусто (или только другие процессы, которые можно остановить)
```

3. **Проверьте, что порт 80 свободен:**
```powershell
netstat -ano | findstr ":80"
# Должно быть пусто (или только другие процессы, которые можно остановить)
```

4. **Запустите Win-ACME снова и получите сертификат:**
```powershell
cd C:\win-acme
.\wacs.exe
```

5. **После успешного получения сертификата запустите Nginx обратно:**
```powershell
# Если Nginx установлен как служба:
Start-Service Nginx

# Или если запускается напрямую:
cd C:\nginx
.\nginx.exe
```

**Альтернативное решение:** Использовать DNS-проверку вместо HTTP (выберите `[Dns]` при настройке Win-ACME), но это сложнее - нужно вручную добавлять TXT записи в DNS.

### Проблема 3: "DNS не резолвится"

**Причина:** DNS записи ещё не обновились  
**Решение:**
- Подождите до 24 часов
- Проверьте настройки DNS в reg.ru
- Используйте `nslookup` для проверки

### Проблема 4: "502 Bad Gateway"

**Причина:** Next.js или FastAPI не запущены  
**Решение:**
```powershell
# Проверить, что приложения запущены
Get-Process node
Get-Process python

# Запустить при необходимости
# Next.js: cd C:\onlyWS\WEB && npm start
# FastAPI: python C:\onlyWS\api_server.py
```

### Проблема 5: "Сертификат истёк"

**Причина:** Автоматическое обновление не сработало  
**Решение:**
```powershell
# Запустить Win-ACME вручную
cd C:\win-acme
.\wacs.exe --renew
```

---

## 📝 Чек-лист выполнения

Отметьте выполненные шаги:

- [x] **Шаг 1:** DNS записи настроены на reg.ru (предположительно, так как сертификат получен)
- [x] **Шаг 2:** Выбран веб-сервер (Nginx)
- [x] **Шаг 3:** Nginx установлен и работает (порт 80 занят Nginx)
- [x] **Шаг 4:** SSL сертификат получен через Win-ACME
  - [x] Сертификат создан для `monitoringcrypto.ru` и `www.monitoringcrypto.ru`
  - [x] PEM файлы экспортированы в `C:\nginx\ssl\`
  - [x] **Файлы созданы:** `monitoringcrypto.ru-crt.pem`, `monitoringcrypto.ru-key.pem`, `monitoringcrypto.ru-chain.pem`
  - [x] Настроена автоматическая задача обновления в Task Scheduler
- [ ] **Шаг 5:** Nginx настроен как reverse proxy с HTTPS ⬅️ **ТЕКУЩИЙ ШАГ**
- [ ] **Шаг 6:** Порты 80 и 443 открыты в файрволе
- [ ] **Шаг 7:** Конфигурация приложения обновлена
- [ ] **Шаг 8:** HTTPS работает и проверен
- [ ] **Шаг 9:** Автоматизация настроена

---

## 📞 Полезные ссылки

- [Nginx для Windows](https://nginx.org/en/download.html)
- [Win-ACME (Let's Encrypt для Windows)](https://www.win-acme.com/)
- [NSSM (установка служб Windows)](https://nssm.cc/)
- [SSL Labs SSL Test](https://www.ssllabs.com/ssltest/)
- [DNS Checker](https://dnschecker.org/)

---

## ⚠️ Важные замечания

1. **Безопасность:**
   - Никогда не делитесь приватным ключом сертификата
   - Храните сертификаты в безопасном месте
   - Регулярно обновляйте Nginx

2. **Резервное копирование:**
   - Сохраните копии конфигурации Nginx
   - Сохраните сертификаты (но не приватные ключи в открытом виде)

3. **Мониторинг:**
   - Регулярно проверяйте логи Nginx
   - Следите за сроком действия сертификата
   - Проверяйте работу сайта

---

**Готово!** После выполнения всех шагов ваш сайт будет работать по HTTPS с зелёным замком 🔒

