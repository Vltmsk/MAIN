# Web Interface для Crypto Spikes Detector

Веб-интерфейс для мониторинга криптовалютных spikes в реальном времени.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск FastAPI сервера

В корне проекта запустите:

```bash
python api_server.py
```

### 3. Запуск Next.js приложения

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📁 Структура

```
Web/
├── app/
│   ├── page.tsx              # Главный дашборд
│   ├── admin/
│   │   └── page.tsx          # Админ панель
│   ├── api/                  # API proxy routes
│   │   ├── spikes/
│   │   ├── metrics/
│   │   ├── users/
│   │   └── health/
│   ├── layout.tsx
│   └── globals.css
├── middleware.ts             # Basic Auth для /admin
└── package.json
```

## 🔐 Админ панель

Для доступа к `/admin` создайте `.env.local`:

```bash
ADMIN_USER=your_username
ADMIN_PASS=your_password
```

## 📡 API Endpoints

Все запросы проксируются через Next.js API routes:

- `/api/spikes` - список spikes
- `/api/metrics` - метрики системы
- `/api/users` - управление пользователями
- `/api/health` - проверка состояния

## 🎨 Технологии

- **Next.js 16** - React framework
- **TypeScript** - type safety
- **Tailwind CSS v4** - стилизация
- **FastAPI** - Python backend

## ✨ Эффекты и анимации

Интерфейс использует современные визуальные эффекты для тёмной темы:

### Glassmorphism эффекты
- **`.glass`** - полупрозрачный фон с размытием для карточек и полей ввода
- **`.glass-strong`** - усиленная версия с большим размытием
- **`.gradient-border`** - градиентные границы при наведении

### Анимации появления
- **`.animate-fade-in`** - плавное появление с лёгким смещением вверх
- **`.animate-scale-in`** - появление с масштабированием
- **`.animate-slide-in`** - появление с движением слева
- **`.animate-float`** - плавающая анимация для карточек

### Эффекты для кнопок
- **`.ripple`** - эффект волны при клике
- **`.hover-glow`** - свечение при наведении (emerald цвет)
- **`.hover-glow-blue`** - синее свечение при наведении
- **`.hover-glow-purple`** - фиолетовое свечение при наведении
- Градиентные фоны: `bg-gradient-to-r from-emerald-500 to-emerald-600`

### Эффекты для карточек
- **`.card-hover`** - плавное увеличение и тень при наведении
- **`.float-animation`** - плавающая анимация
- Цветные тени: `.shadow-emerald`, `.shadow-blue`, `.shadow-purple`

### Эффекты для форм
- **`.input-focus`** - свечение границ при фокусе (emerald цвет)
- **`.smooth-transition`** - плавные переходы для всех интерактивных элементов

### Эффекты для таблиц
- **`.table-row-hover`** - подсветка строки с цветной границей слева при наведении

### Специальные эффекты
- **`.gradient-text`** - анимированный градиентный текст для заголовков
- **`.gradient-bg`** - анимированный градиентный фон страниц
- **`.status-pulse`** - пульсация для активных статусов
- **`.nav-active`** - индикатор активного пункта навигации с подчёркиванием

### Loading состояния
- Skeleton loaders с анимацией shimmer
- Анимированные спиннеры загрузки

### Кастомные классы в globals.css

Все эффекты определены в `app/globals.css`:

```css
/* Основные анимации */
@keyframes fadeIn, slideIn, scaleIn, pulse, float, shimmer, gradient, ripple

/* Утилиты */
.animate-fade-in, .animate-slide-in, .animate-scale-in
.animate-pulse-slow, .animate-float

/* Glassmorphism */
.glass, .glass-strong

/* Эффекты */
.gradient-border, .hover-glow, .card-hover, .ripple
.skeleton, .gradient-text, .input-focus
.scroll-reveal, .smooth-transition, .nav-active
.shadow-emerald, .shadow-blue, .shadow-purple
.status-pulse, .float-animation, .gradient-bg
.table-row-hover
```

## 🔧 Разработка

```bash
npm run dev      # dev server
npm run build    # production build
npm run start    # production server
npm run lint     # линтер
```

## 📝 Подробная документация

Все эффекты и анимации описаны в этом README. Для развёртывания см. основной [README.md](../README.md) в корне проекта.
